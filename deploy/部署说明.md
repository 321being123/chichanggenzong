# 持仓管理系统 · 腾讯云部署说明

> 适用：把本地运行的持仓管理系统（Node + PostgreSQL + Express）部署到腾讯云 CVM（Linux）。
> 前置：已修改好的 `server.js`（CSRF 读 `ALLOWED_ORIGIN`、启用 `trust proxy`、cookie `secure:auto`）已就位。
> 存储已从 SQLite 改为 PostgreSQL（用户 2026-07-08 决策，计划开放给几十人使用）。

## 零、每次更新部署的强制流程

1. 本地完成代码修改。
2. 运行语法检查、自动测试和本次功能的关键回归测试。
3. 等待用户明确说“部署”或“部署到线上”；未获得明确许可时禁止推送和操作线上服务器。
4. 更新 `public/changelog.json` 和根目录 `CHANGELOG.md`，补齐本次版本记录。
5. 提交代码并推送 GitHub `master`。
6. 登录腾讯云，检查并保留服务器自动生成的日报、个股报告和本地配置文件。
7. 快进拉取代码、更新 Nginx 配置、安装生产依赖、重启生产服务器的 systemd 服务。
8. 验证提交版本、systemd 服务状态、`/health` 以及本次改动涉及的线上接口。

### 本项目固定登录方式

- **腾讯云生产服务器**：`ubuntu@82.156.125.47`。**已关闭密码登录（2026-07-31 安全加固）**，只允许 SSH 密钥登录，密钥为本机 `~/.ssh/server_login`（环境变量 `SSH_KEY_PATH` 可覆盖路径）。服务器 sudo 已配置 NOPASSWD，脚本内直接 `sudo` 提权，全程不依赖密码。不要尝试本机的 `id_ed25519` 或 `temp_cvm_key`。
- **唯一部署命令**：首次在本机安装部署依赖：`python -m pip install -r deploy/requirements-deploy.txt`；以后每次确认代码已推送到 GitHub 后，只执行 `python deploy/deploy_password.py --confirm-production`。脚本会用 `~/.ssh/server_login` 密钥登录，备份并更新 `/etc/nginx/conf.d/portfolio.conf`、执行 `nginx -t` 和 reload，再执行 `git fetch`、`git reset --hard origin/master`、生产依赖安装、systemd 服务重启与健康检查；任一步骤失败会回滚代码、Nginx 配置和服务状态。密钥只在本机，不显示、不写入日志或提交到仓库。
- **GitHub**：远程仓库为 `git@github.com:321being123/chichanggenzong.git`，使用本机 `~/.ssh/id_ed25519` 和 `~/.ssh/known_hosts`。Windows PowerShell 推送命令：

  ```powershell
  $env:GIT_SSH_COMMAND='ssh -i "C:/Users/戴存在/.ssh/id_ed25519" -o UserKnownHostsFile="C:/Users/戴存在/.ssh/known_hosts" -o StrictHostKeyChecking=accept-new -o BatchMode=yes'
  git push origin master
  ```

- **固定部署顺序**：标准脚本登录后进入 `/opt/portfolio`，先备份并安装 Nginx 模板，`nginx -t` 通过后 reload，再执行 `git fetch origin`、`git reset --hard origin/master`、`npm ci --omit=dev`、systemd 服务更新和重启。重启后需等待服务启动完成，再检查提交版本、Web/Worker/健康检查定时器状态和 `http://127.0.0.1:3000/health`；刚重启瞬间端口未监听属于启动过程，不要因此重复改登录方式。

## 一、服务器端准备

1. **买 CVM**：选 Ubuntu 22.04 / 24.04（或 CentOS 7+），地域随意。
2. **装 Node**：服务器 Node 版本必须与本地一致（当前 **22.x**）。
   ```bash
   curl -fsSL https://deb.nodesource.com/setup_22.x | sudo -E bash -
   sudo apt-get install -y nodejs
   node -v   # 应显示 v22.x
   ```
3. **装 PostgreSQL + Nginx + Python**（生产进程由 systemd 管理）：
   ```bash
   sudo apt-get install -y postgresql nginx python3 python3-venv python3-pip
   ```
4. **建库 + 建专用账号**（不要用默认 postgres 超级用户跑业务）：
   ```bash
   sudo -u postgres psql <<'SQL'
   CREATE DATABASE portfolio;
   CREATE USER portfolio_user WITH PASSWORD '改成强密码';
   GRANT ALL PRIVILEGES ON DATABASE portfolio TO portfolio_user;
   -- 授权 public schema（PostgreSQL 15+ 需单独授权）
   \c portfolio
   GRANT ALL ON SCHEMA public TO portfolio_user;
   SQL
   ```
   > 服务启动时会由 `server/db.js` 的 `initSchema()` 自动建表（users / account_data / positions / trades / nav_history / cash_flows），无需手动建表。
   > 若数据库启用 SSL（云数据库常见），在 `.env` 设 `PGSSL=true`，并改用 `DATABASE_URL` 连接串。

## 二、上传代码

> **历史备用方案（不用于当前生产）**：以下 tar/scp/root 登录流程仅保留作回溯。
> 当前禁止直接覆盖生产代码或使用 root 登录；请回到本文“零、每次更新部署的强制流程”，
> 通过 `deploy/deploy_password.py` 和 Git/systemd 完成部署。

**不要**直接把 Windows 上的 `node_modules` 拷上去（原生模块平台不兼容，在服务器上用 `npm install` 重装）。

```bash
# 本地打包（排除 node_modules、data、.env、.git）
cd portfolio-server
tar --exclude=node_modules --exclude=data --exclude='.env' --exclude='.git' -czf ../portfolio.tar.gz .

# 传到服务器（替换为你的公网 IP）
scp ../portfolio.tar.gz root@<服务器公网IP>:/root/

# 服务器解压
ssh root@<服务器公网IP>
mkdir -p /opt/portfolio && tar -xzf /root/portfolio.tar.gz -C /opt/portfolio
cd /opt/portfolio
npm install          # pg 是纯 JS 驱动，无需编译原生模块，比 better-sqlite3 更简单
python3 -m venv ipo-report/venv
ipo-report/venv/bin/pip install -r requirements.txt  # 打新日历 18:00 定时任务
```

> 数据迁移：本系统已彻底脱离 SQLite，无旧 SQLite 文件可迁。全新部署直接启动即可，`server.js` 启动会自动建表（`initSchema()`）。
> 若需从 CSV 批量导入持仓，用 `import_positions.py`（需 `pip install -r requirements.txt`）。

## 三、配置环境变量

> 应用启动时通过 `dotenv` 自动读取项目根目录的 `.env`（无需手动 export），填好即生效。

```bash
cd /opt/portfolio
cp deploy/.env.example .env
nano .env            # 填写 SECRET、ALLOWED_ORIGIN、REGISTER_CODE
```

- `SECRET`：`openssl rand -hex 32` 生成一个
- `ALLOWED_ORIGIN`：改成你的域名，如 `myapp.com,www.myapp.com`
- `REGISTER_CODE`：设一个邀请码（公网必设）
- **PostgreSQL 连接**（必填）：在 `.env` 填 `DATABASE_URL=postgres://portfolio_user:密码@localhost:5432/portfolio`，或分项填 `PGHOST`/`PGPORT`/`PGUSER`/`PGPASSWORD`/`PGDATABASE`/`PGSSL`（详见 `deploy/.env.example`）。

## 四、Nginx 反代 + HTTPS

```bash
cp deploy/nginx-portfolio.conf /etc/nginx/conf.d/portfolio.conf
# 把里面的 myapp.com 和证书路径改成你的实际值
nginx -t && systemctl reload nginx
```

证书：腾讯云控制台申请**免费 SSL**（DNSPod 证书），下载 Nginx 版，放到 `/etc/nginx/ssl/myapp.com/`。

## 五、systemd 服务

标准部署脚本会安装并启用 `portfolio-server.service`、
`portfolio-worker.service` 和 `portfolio-worker-health.timer`，不要使用 PM2。
手动检查或重启：

```bash
systemctl is-active portfolio-server.service portfolio-worker.service portfolio-worker-health.timer
sudo systemctl restart portfolio-server.service portfolio-worker.service
```

## 六、腾讯云安全组

只放行 `80`、`443`、`22(SSH)`。**不要**对公网开放 `3000` 端口——它只由 Nginx 在服务器内部访问。

## 七、验证

1. 浏览器打开 `https://你的域名/`，应跳转到登录页且地址栏有锁。
2. 注册/登录/刷新行情/保存数据均正常（不再 403）。
3. 使用 `journalctl -u portfolio-server.service` 查看日志；重启命令见上面的 systemd 小节。

## 八、备份（重要）

数据在 PostgreSQL，用 `pg_dump` 备份：
```bash
# 手动
pg_dump -U portfolio_user portfolio > /backup/portfolio-$(date +%F).sql

# 每日 03:00 自动（cron）
0 3 * * * /usr/bin/pg_dump -U portfolio_user portfolio | gzip > /var/backups/portfolio_$(date +\%F).sql.gz
```
也可用**云硬盘快照**作为兜底。

## 九、时区与子目录部署（已实现）

- **时区（东八区）**：所有业务日期（净值日期、交易/现金流日期、导出文件名）已统一用北京时间，不再受服务器时区影响；K 线起止日也用北京时间。
- **子目录部署（可选）**：前端接口与静态资源已支持放在域名子目录（如 `https://域名/portfolio/`）。
  开启方法（两步）：
  1. 把两个 HTML 里 `<meta name="base-url" content="">` 的 `content` 改为子目录前缀，例如 `content="/portfolio"`（**必须以 `/` 开头、结尾不带 `/`**）。
  2. Nginx 用带 trailing slash 的 location 反代，把前缀剥掉：
     ```nginx
     location /portfolio/ {
         proxy_pass http://127.0.0.1:3000/;   # 结尾的 / 会把 /portfolio 前缀去掉再转发
         proxy_set_header Host $host;
         proxy_set_header X-Real-IP $remote_addr;
         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
         proxy_set_header X-Forwarded-Proto $scheme;
     }
     ```
  根目录部署（默认）保持 `content=""`，Nginx 仍用根 location 即可，无需改代码。
