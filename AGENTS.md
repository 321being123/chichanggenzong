# 项目后续功能开发强制约束

## 项目长期记忆与架构文档

- 本项目的统一记忆入口是 `docs/知识索引.md`，机器路由是 `governance/knowledge-map.json`。每次任务先读索引，再按任务描述和实际改动文件匹配路由；禁止默认全量读取历史文档。
- 命中路由后必须阅读其 `read` 列出的权威文档；实际改动触发的 `update` 文档必须同步更新。根因不明、跨模块、数据、权限、安全、调度或部署问题必须扩大阅读范围。
- Bug 必须从数据源、数据库、服务、接口、页面和运行环境检查根因，并核查同类对象/功能；重大或重复问题按 `docs/incidents/README.md` 留下根因、同类范围和回归证据。
- 交付前运行 `npm.cmd run check:knowledge`；涉及代码、依赖、数据库、配置或部署行为变化时运行 `npm.cmd run test:all`。若最终只改版本号、`CHANGELOG.md` 或 `public/changelog.json`，可只重跑版本一致性、知识门禁和本地运行验收；本项目事实只写入 Git 仓库，平台私有 Memory 不作为事实来源或裁决依据。Codex 项目级原生 Memory 已在 `.codex/config.toml` 关闭。
- 多 Agent 协作时，项目事实仍然只写 Git 仓库。WorkBuddy 的记忆路径（`工作区 .workbuddy/memory/`、用户级 `~/.workbuddy/MEMORY.md`、云端画像与历史会话检索）只做接续指针，冲突一律以本仓库为准；完整路径清单与写什么、禁写什么见工作区根目录 `AGENTS.md` 的"多 Agent 协作：记忆路径统一规则"。
- `docs/技术架构.md` 是整个项目技术架构的权威入口，也是后续开发和维护的长期项目记忆；开始涉及架构、数据、接口、任务、配置或部署的工作前，必须先阅读相关章节。
- 每次修改影响技术栈、目录职责、请求链路、数据模型、读写边界、外部数据源、后台任务、配置项或部署方式时，必须在交付前同步更新 `docs/技术架构.md` 的原有对应章节及“最后核对日期”。
- 文档必须以当前代码和已验证运行状态为准；发现不一致时就地修正，不另建内容重复的架构文档。单次整改过程和历史记录继续放整改报告或 `CHANGELOG.md`。

## 方案设计强制门禁

- 凡用户要求“方案”“根本解决方案”“架构设计”或“重构建议”，即使本次不修改代码，也必须先读取 `AGENTS.md`、`docs/知识索引.md`、`governance/knowledge-map.json`，再按主题读取命中的权威架构文档和专项方案。
- 涉及港股打新、港股 IPO、暗盘、基石投资者或港股申购建议时，必须额外读取 `docs/港股打新日历与严选建议研发方案_20260907.md`；未达到该方案的历史回测门禁前，禁止把研究评分宣传为正式申购建议。
- 给出方案前必须核对当前代码、任务定义、数据链路和运行边界，并先明确“已有能力、可复用能力、真正缺口”；禁止仅凭问题描述直接设计新链路。
- 方案必须优先扩展现有架构，禁止新增平行任务、平行数据源、平行事实表或第二套调度器；确需新增时，必须说明现有能力为何不能承载及影响范围。
- 每份方案必须写明架构对齐结论、数据源→数据库→服务→接口→页面/任务的影响链路、验收方式，以及需要同步的权威文档。
- 涉及后台告警收敛时，关闭条件必须绑定可查询的作用域和恢复证据：空查询不能视为恢复，数据质量告警不能被普通任务成功代替，通配来源熔断必须按实际关闭的 `source:*` 作用域处理；历史整改过程写入事故文档，不堆入本规则文件。
- 涉及可转债强赎时，官方不强赎决定及其有效锁定期必须优先于股价数学触发结果；锁定期内不得显示“已满足待确认”，截止日后的重新计数必须使用本地交易日历。旧公告回放、解析版本和部分失败必须有独立可查水位，不能用共享公告游标或任务整体成功代替。

## 全局表格样式

- 所有新增或修改的业务数据表格统一沿用“上市可转债”列表样式，完整规范以 `docs/技术架构.md` 的“全局业务表格规范”为唯一权威说明。
- 表格样式和滚动能力必须优先沉淀到 `public/shared/style.css` 与共享前端能力，禁止在不同业务 CSS/JS 中复制多套近似实现；页面专属文件只保留字段宽度等业务差异。
- 现有表格按实际修改范围逐步对齐，禁止仅为统一外观而一次性重构所有页面。
- 强赎监控、下修监控和可转债安全性属于同一功能族，标题区、统计卡片、工具栏、输入框和更新时间必须复用 `public/shared/style.css` 的 `.bond-feature-*` 规范；新增可转债功能不得另起颜色、字体或控件尺寸。

## 前端移动端适配开发标准（强制）

- 以后所有新增或修改的前端页面、组件和交互，必须同时适配手机与桌面；不得只完成桌面页面后把移动端作为可选待办。规范以 `docs/技术架构.md` 第 5.3 节为准。
- 正文手机分界统一为 760px，导航收起分界为 900px；复用 `public/shared/style.css`、`mobile-ui.js`、`business-table.js`、`chart-interaction.js` 和既有页面状态，禁止另建移动端页面或复制权限、计算、取数链路。
- 手机正文应重排；宽表格/密集图表只在各自容器横滑，保留完整金融字段和数值；操作区域至少 44px，输入字号 16px；菜单、图表提示、表单和弹窗不能依赖鼠标悬停。
- 每次前端交付检查本次改动及同类页面的 320/360/390/430/768/1024/1440px 布局、760/900px 分界、触屏与键盘、桌面回归及原权限。屏幕旋转、菜单展开和图表点按不得额外取数。
- 执行 `npm.cmd run test:all`（含移动端行为回归）及 `npm.cmd run check:knowledge`；同步第 5.3 节和相关任务的实际验收范围。iOS Safari、Android Chrome、微信内置浏览器未实测时不得宣称已通过真机兼容。
- 遵守用户的低性能模式：未获得允许时不打开浏览器、不截图、不跑视觉测试、不启动开发服务器；以允许的自动检查推进，并在交付中明确未验证项，禁止用静态检查冒充真机验收。

## 固定部署登录流程

## 本地运行版本闭环（每次代码修改均强制执行）

- Windows 下所有 npm 命令一律直接调用 `npm.cmd`（如 `npm.cmd run test:all`），禁止先尝试 `npm` 或 `npm.ps1`；本机 PowerShell 执行策略会稳定拦截 `npm.ps1`，重复尝试只会产生无效报错。
- 每次完成代码修改并通过相关测试后，必须自动重启或刷新本地服务，使运行中的服务加载最新代码；禁止只修改文件、不更新本地运行状态。
- 每次本地交付前必须检查 `http://127.0.0.1:3000/health`、本次相关页面或接口以及静态资源版本；三者与当前代码一致后才能宣布完成。
- 需要形成新版本时，默认递增 `appVersion` 最后一位，并同步 `package.json`、`package-lock.json`、`CHANGELOG.md` 和 `public/changelog.json`。本地 `/health` 返回旧版本时，视为任务未完成。

## 本机 PostgreSQL 固定信息（每次本地验收前必读）

- 本机唯一现役 PostgreSQL 实例为 17.2（Windows x64 免安装版），程序目录为 `E:\pgsql\bin`，数据目录为 `E:\pgdata`，监听 `localhost:5432`，本地业务库为 `portfolio`。
- 该实例未注册为 Windows 服务，且未加入系统 `PATH`。`Get-Service *postgres*` 无结果或 `Get-Command psql` 找不到命令，均不能作为“本机未安装 PostgreSQL”的依据。
- 在得出“本机没有数据库”的结论前，必须先检查 `E:\pgsql\bin\pg_ctl.exe`、`E:\pgsql\bin\psql.exe` 和 `E:\pgdata\PG_VERSION`；文件存在但实例未运行时，应按需启动，不能重新安装。
- 数据库连接账号、密码等敏感配置只从项目根目录 `.env` 读取，禁止把密码复制进规则、文档或代码。
- 启动、状态检查、连接和停止命令见 `deploy/本地验证交接文档.md` 的“本机 PostgreSQL 固定信息”章节。

## QQ 邮箱 SMTP 固定信息

- 用户已确认 QQ 邮箱的 SMTP 服务已开启（2026-08-13）。项目根目录 `.env` 已配置 `SMTP_HOST/SMTP_PORT/SMTP_USER/SMTP_PASS` 和 `ALERT_EMAIL_TO`。
- 排查邮件告警时，不得再默认判断为“QQ 邮箱未开启 SMTP”；应先检查应用 SMTP 连接、QQ 邮箱授权码、网络连通性和投递记录。
- SMTP 授权码及邮箱账号等敏感值只允许保存在 `.env`，禁止复制到规则、Markdown、代码、日志或回复中。

## 生产部署铁律（任何服务器部署均强制执行）

以下流程适用于所有生产服务器部署、热修复、数据同步和模型更新，不得跳步：

1. **先更新版本记录**：先用 `add_changelog.py` 同步更新 `public/changelog.json`、`CHANGELOG.md`；需要发版号时同时更新 `package.json` 的 `version/appVersion`。未完成版本记录，禁止部署。
   - Windows 固定使用项目根目录 `.\venv\Scripts\python.exe add_changelog.py ...`，禁止使用 `WindowsApps\python.exe` 占位程序，也不要猜测 `ipo-report\venv` 路径。
2. **本地验证**：完成与改动相关的测试和数据校验；代码、依赖、数据库、配置或部署行为变化须运行全量测试，纯版本/变更记录元数据变更无需重复全量测试。
   - Windows 全量测试固定使用 `npm.cmd run test:all`，外层超时至少 240 秒，避免 `npm.ps1` 执行策略错误和正常测试被提前终止。
3. **正式提交并推送**：所有代码、版本记录、模型文件和同步 SQL 必须提交 Git 并推送到 `master`；禁止只保留本地修改。
4. **标准服务器部署**：仅按本文件规定的 Git 同步、依赖安装和 systemd 重启步骤执行。禁止通过 SFTP、SSH 直接覆盖生产代码或模型文件；紧急修复也必须补齐本流程后才算完成。
5. **部署后验收**：核对提交版本、Web/Worker/健康检查三个 systemd 单元、`/health`、本次相关接口/页面和数据库同步结果；任一项失败即视为部署失败。

数据同步不是固定步骤。只有本次变更涉及数据库结构、历史数据、日报、模型产物或其他需要写入生产数据的内容时，必须先询问用户是否执行同步；未获得明确同意，禁止生成同步 SQL 或执行数据同步。

完整命令顺序与验收标准见 `docs/生产部署流程.md`。本节优先级高于任何旧部署说明。

- 项目采用四位应用版本号。统一以 `package.json` 的 `appVersion` 为线上代码版本和程序版本；npm 必需的 `version` 使用兼容写法。以后每次部署都必须更新 `appVersion`，用户未指定新版本时默认递增最后一位，并在部署后核对 `/health` 返回的版本号。规则文档不再写死当前版本，避免与 `package.json` 漂移。

- 腾讯云生产服务器：`ubuntu@82.156.125.47`。`root` 账号不允许直接登录，不要再次尝试。**已关闭密码登录（2026-07-31 安全加固），只允许 SSH 密钥登录**：使用本机 `~/.ssh/server_login`（环境变量 `SSH_KEY_PATH` 可覆盖），通过 Paramiko 的 Ed25519Key 认证；服务器 sudo 已配置 NOPASSWD，执行部署命令时直接用 `sudo` 提权，全程不需要密码。不要使用密码、不要尝试本机 `id_ed25519` 或 `temp_cvm_key`。
- GitHub：仓库 `git@github.com:321being123/chichanggenzong.git`，使用本机 `~/.ssh/id_ed25519` 和 `~/.ssh/known_hosts`。若直接 `git push` 出现主机密钥校验失败，使用：
  `GIT_SSH_COMMAND='ssh -i ~/.ssh/id_ed25519 -o UserKnownHostsFile=~/.ssh/known_hosts -o StrictHostKeyChecking=accept-new -o BatchMode=yes' git push origin master`
- 部署时先推送 GitHub，再使用 `deploy/deploy_password.py` 按标准流程登录腾讯云并部署；生产由 systemd 管理 `portfolio-server.service`、`portfolio-worker.service` 和 `portfolio-worker-health.timer`，PM2 已停用。重启后检查提交版本、三个 systemd 单元、`http://127.0.0.1:3000/health` 和本次相关接口。服务器已关闭密码登录，统一使用 `~/.ssh/server_login` 密钥连接，不要再尝试密码登录。

以后新增或修改金融数据、数据库及产品功能时，必须同时满足以下要求：

## 1. API调用与增量更新

- 外部接口数据首次完整拉取后必须落库，页面和重复计算优先读取数据库，不能因用户查看、切换图表或刷新页面重复调用外部接口。
- 后续默认按同步游标、公告日期、交易日期或报告期增量拉取，只保留必要的短重叠窗口处理更正和漏数。
- 接口空值、异常值或失败不得覆盖数据库中的上一份有效数据。
- 异常数据优先进行一次定点重拉；仍异常时记录数据质量问题，禁止无限重试。
- 新接口接入前必须说明首次全量范围、日常增量范围、缓存周期、去重键、补漏方式和失败保留策略。

### 1.1 公告数据源优先级与统一采集规范（P0-1，强制）

- 公告读取必须按“本地完整事实 → 本地原文/PDF重解析 → 未覆盖窗口的外部增量采集 → 允许的备源”执行。判断本地是否可用时必须同时检查完整性、时效、解析版本和质量状态，不能只判断数据库中是否存在一行记录。
- 页面、API 和业务计算默认只读已落库的 `ops.raw_records`、`event.documents` 及其标准事实/派生表；不得在页面请求、重复计算或业务任务内为同一公告直接重复调用外部接口。
- 公告“发现新内容”是例外场景：先检查来源、接口和业务日对应的本地游标/覆盖水位，只查询未覆盖的时间窗口；查询成功后先统一落库，再由业务模块分类和解析。成功空结果可以推进已核验水位，但失败、分页不完整或结果不确定时不得推进游标。
- 数据源优先级按公告事实执行：上交所/深交所/北交所/港交所等官方交易所为主源；巨潮资讯仅在对应任务契约明确允许时作为资料备源或兜底；Tushare 等末级备源不得改变“官方公告优先”的事实口径。官方主链明确禁止回退时，不得自行切换巨潮。
- 公告采集、原始响应、来源游标、唯一键去重、PDF/正文缓存和失败重试必须由统一采集层承载。跨可转债、套利、IPO 等业务不得各自复制交易所查询、下载和去重逻辑；业务任务可以按时段独立编排，但只能消费统一公告库。
- IPO 招股书、港股配发/取消上市资料、可转债上市流通/发行结果等官方 PDF 必须使用 Node/Python 共用的持久化缓存（默认 `data/document_pdf_cache`，URL SHA-256 文件键，原子写入，默认 30 天/5 GiB 清理）；命中缓存不得联网，缓存只保存原文，业务事实仍写入 `ops.raw_records`/`event.documents`。缓存损坏或过期按未命中处理，解析失败不得覆盖有效事实。
- 公告唯一性至少按“来源＋来源原生编号”约束；无原生编号时使用规范化 URL/内容哈希，并保存来源、公告时间、证券/公司身份、原始地址、文件哈希、正文哈希、解析版本和质量状态。补发、更正和重解析必须保留可追溯关系，不能用空值覆盖有效事实。
- 后续新增公告需求必须先登记来源、接口、市场范围、筛选条件、主备顺序、游标/覆盖水位、唯一键、缓存周期、补漏方式、失败保留策略和下游消费者，并接入统一采集层；未经评审不得在业务模块直接新增外部公告请求、平行事实表或平行定时任务。

### 1.2 接口限制与熔断规范（P0-2，强制）

- 只配置已经核验的具体接口限制；来源级、Token 级或全局上限在没有可靠依据时必须保持空值，不能为了“安全”自行猜一个数字。官方限制、本站内部等待和任务批次止损必须分开记录。
- `ops.source_endpoint_policies.internal_per_minute_limit/internal_daily_limit` 强制保持 `NULL`；官方文档或真实上游响应确认的限制只写 `official_*`。任务契约不得用 `maxExternalCallsPerRun`、`slotExternalCallsLimit`、`dailyBudget` 编造调用额度；未知时保持空值，使用任务超时、连续无进展和持久化剩余进度保护。
- 单接口限流、单接口权限拒绝只影响该接口；只有确认是凭据整体失效或确实属于全局凭据问题时，才允许扩大到凭据级。普通接口错误不得熔断整个来源、Token 或其他接口。
- 内部预算或最小间隔达到保护线时进入等待，不开启熔断；任务批次达到安全止损时只停止当前槽位，不开启来源熔断；只有真实上游 429、额度耗尽等对应接口异常才允许开启接口熔断。
- 熔断状态只能统一写入 `ops.external_circuits`，业务模块、Python 子进程和脚本不得另存本地计数器或永久熔断状态。Node、Python、脚本必须共用同一接口策略、凭据指纹和熔断记录。
- `state='open'` 的临时熔断必须保存明确的 `recover_at`；到期后自动探测，验证成功自动关闭对应作用域。确认无权限时写接口权限阻塞并停用对应策略，不得保留为无恢复时间的 open 熔断；手工改状态也不能代替恢复证据。

### 1.3 自动更新单一链路规范（P0-3，强制）

- 同一个外部接口、同一个业务窗口只能有一个采集入口；其他功能只读数据库或统一缓存，不得重复联网。新增任务前必须先证明现有任务无法承载，并说明影响范围。
- 页面打开、切换子页、普通定时刷新和重复计算不得偷偷触发外部采集；只有明确的人工刷新动作可以进入受控入口，并且仍受接口策略、批次止损和审计约束。
- 自动任务、人工补跑、启动补偿和离线脚本必须调用同一个 Runner/服务入口，不能各自复制一套请求、重试、落库或状态判断。
- 下游只依赖具体业务日的数据集分区和质量状态，不依赖“上游整个任务是否成功”这一笼统状态；任务编排必须声明生产和消费的数据集。

### 1.4 任务完成与数据质量规范（P0-4，强制）

- 任务只有同时满足以下条件才允许标记成功：结果明确成功；所有声明的数据集已发布；分区日期等于目标业务日；质量检查通过；没有未完成子阶段或失败对象。
- “接口成功但返回空”必须与“接口失败导致没有数据”严格区分。无新增数据只能发布带证据的 `verified_no_change`，不能把未知或失败伪装成零行成功。
- 部分完成必须持久化剩余进度并续跑；主数据成功不能掩盖子数据集失败，也不能把整个任务标记为成功。执行状态、数据质量状态和业务状态必须分别保存和展示。

### 1.5 禁止隐性业务上限规范（P0-5，强制）

- 候选查询、资料补全和历史回填不得写死“只处理若干条、若干只、若干页、若干天”等业务上限后仍宣告完成。分批只是执行方式，不是完成条件。
- 确需分批时，必须同时保存持久化剩余队列或游标、剩余数量、下一次续跑时间和未完成阶段，并且未清空剩余工作前不得标记成功；续跑只处理未完成阶段。
- 槽位最大运行时间、调用总量、无进展次数和批次数可以作为防死循环的安全止损，但它们不等同于接口额度、来源熔断或业务完成上限。达到止损必须进入等待、部分完成或人工阻塞，并保留原因。

### 1.6 日期、游标与目标对象规范（P0-6，强制）

- 任务目标日必须由调度器传递到所有子阶段；子阶段不得改用服务器当前日期。所有业务日期统一按上海时区处理，禁止直接对 PostgreSQL `Date` 或 JavaScript `Date` 做 UTC 字符串截断。
- 游标只能单调前进；补旧数据不得回退较新的成功水位。`MAX(date)` 只能说明最新日期，不能证明中间没有断档，历史补漏必须检查日期差集和数据集分区。
- 目标对象必须按目标日的真实生命周期、市场和业务范围筛选，不能用陈旧的 `listed` 状态、历史档案或当前日期集合替代目标日集合。对象集合去重和排除原因必须可审计。

### 1.7 告警恢复规范（P0-7，强制）

- 告警必须绑定准确作用域：计划槽位、任务、数据集分区，或来源＋接口＋凭据。恢复关闭只能作用于实际匹配的作用域。
- 数据质量告警只能由对应数据集的发布和质量恢复证据关闭，不能被普通任务成功代替；来源告警必须同时确认相关熔断已关闭并产生新的成功时间。
- 空查询、手工改状态、只有日期水位、单次普通任务成功都不能作为恢复证据。恢复通知必须按告警键去重，禁止批量清零或无证据关闭历史告警。

## 2. 数据库冗余与扩展

- 数据库设计必须考虑未来增加字段、市场、币种、数据源和历史版本，避免只为当前页面设计一次性表结构。
- 高频查询、筛选、排序和计算字段使用正式列；完整上游响应及暂未标准化字段保存在 `JSONB`，作为合理冗余和追溯依据。
- 原始数据、标准化事实、计算指标和页面快照分层保存，不能只保存最终页面JSON。
- 使用内部证券ID、公司ID和数据源ID关联，避免把单一市场代码或单一接口字段作为长期架构核心。
- 设计迁移时必须包含唯一约束、索引、单位、币种、时间口径、版本字段和数据质量约束。

## 3. 整体数据与产品架构

- 新增功能前必须检查它与现有采集、数据库、指标计算、接口、页面、任务调度、权限和缓存的关系，禁止孤立增加重复链路。
- 优先复用现有公共组件、数据源适配器、同步游标、质量记录和指标定义，不重复实现相同能力。
- 新功能不得破坏持仓、交易、打新日历、可转债、用户隔离及其他现有业务。
- 数据结构和功能代码必须支持兼容扩展；涉及替换旧结构时，先完成新链路验证，再进行明确范围的迁移或删除。
- 每次交付必须验证：增量更新、数据去重、失败保留、历史追溯、接口兼容、数据库迁移及相关自动测试。

## 4. 单个案例与全局同类问题核查

- 用户以某一只转债、股票、基金、某一条数据、某个页面、某项功能或其他单个对象作为问题案例时，该案例仅用于定位问题，不代表修复范围只限该对象。
- 修复后必须使用同一套规则核查其他持仓、自选、页面、功能及相关同类数据是否存在相同问题；适用时应进一步核查全部同类对象。
- 发现同类问题必须一并修复，不得等待用户逐只指出。
- 每次交付必须明确说明核查范围、通过数量及仍存在的异常；未完成同类核查不得宣布任务完成。

## 5. 解析、身份与发布验收规范（P1，强制）

- 公告解析必须按目标公司、证券代码、市场、币种和时间线隔离；禁止只靠宽泛关键词、取正文最后一个数字或跨公司/跨市场拼接条款。官方决定优先于股价数学推算，解析必须保存原文证据、来源和版本。
- 证券代码、公司和供应商映射只能经过统一身份服务；业务模块不得自行拼接市场后缀、复用不同环境的数字 ID 或在无唯一匹配时猜测代码。
- 发布验收不能只看测试通过和 `/health`。必须核对提交版本、静态资源、目标接口、页面实际字段、任务运行记录、数据分区/质量状态和同类对象范围；未实测的生产或真机能力不得宣称已通过。

## 6. 自动门禁（强制）

- 外部请求必须经过统一 Guard；业务模块不得直接维护预算、熔断或恢复计数。数据源顺序测试必须验证“交易所先于巨潮”，并覆盖失败、分页不完整和解析失败分支。
- 分批上限测试必须同时验证持久化续跑状态、剩余数量、下一次时间、未完成判定和槽位止损；任务测试必须验证声明的数据集全部注册、发布并通过质量门禁。
- 日期相关静态检查和回归测试必须阻止任务代码使用 UTC 截断业务日期；页面默认请求测试必须阻止携带强制外部刷新参数。
- 同类问题第三次出现后，必须把事故特例升级为通用不变量检查，不能继续只增加单个事故测试。`check:knowledge` 负责知识路由和规则同步，业务不变量由通用测试/静态检查负责；未实现的自动门禁必须标记为待补齐，不得以文字规则冒充已完成。

以上 API、公告、任务、质量、日期、告警、解析和发布规则是后续研发的必检项，不得省略。
