node_modules/
data/portfolio.db
data/.secret
server/data/
.env
.env.local
*.log
.DS_Store
Thumbs.db

# Python 缓存
__pycache__/
*.pyc

# Python 虚拟环境（本地/CI 生成，不入库）
ipo-report/venv/
venv/

# 已弃用代码存档（不入库，本地保留）
_deprecated/

# SQLite 残留 / 临时备份
data/json_backup_*/
data/*.db-shm
data/*.db-wal

# 打新日历模块：SQLite 迁移源（仅本地迁移用，不入库）
ipo-report/data/*.db
ipo-report/data/*.db-shm
ipo-report/data/*.db-wal

# 测试脚本（含敏感信息风险，不入库）
test_verify.js

# Windows 保留设备名文件（误生成会导致 rg 等工具报错，禁止入库）
nul
nul.*

# 本地调试脚本 / 临时产物（不入库）
debug_*.js
debug_*.html
*.bundle
tp.log
tp_out.log
_探针_*
_probe/

# 数据库备份产物（含加密/校验文件，禁止入库）
backups/
*.sql.gz
*.gpg
*.sha256
