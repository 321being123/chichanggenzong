# 任务：可转债下修监控发布收尾

状态：已关闭
Git 基线：8fafd1cd7bf516f3b0d6a1b42cfcd8a3e8a23503

## 目标与边界

核对下修监控修复的代码、线上服务、生产数据补算和项目知识记录是否一致。仅补齐文档与收尾凭证，不删除待确认的临时文件、旧报告或工作树。

## 已读取知识与证据

- 已读取 `AGENTS.md`、`docs/知识索引.md`、`governance/knowledge-map.json`、`docs/技术架构.md`、`docs/生产部署流程.md`、`docs/incidents/README.md`、`docs/tasks/README.md` 及洁癖技能要求。
- 本地 `master` 与 `origin/master` 基线为 `8fafd1c`；知识门禁 `npm.cmd run check:knowledge` 通过。
- 线上版本 `0.6.4.29`；Web、Worker、健康检查定时器均正常，`/health` 正常。
- 线上已对缓存中的官方公告 PDF 重解析并完成 310/310 派生重算；火星转债锁定至 2026-09-30，下一观察期为 2026-10-01。

## 已确认事实、已排除假设

- 已排除“火星转债仍在已满足待公告是计算未重置”的假设：线上状态已为 `locked`，并带有公告锁定结束日和下一起算日。
- 已排除“振26上市前行情被计入”的问题：观察窗口按上市日计算。
- 已排除净资产底线券继续进入待公告/接近触发的问题：线上 `floor_blocked` 共 5 只，未混入上述状态。
- 未宣称上游全量公告同步完成：线上仍为 `stale=true`、`announcement_errors=1`，公告源 Token 熔断是未闭环项。

## 当前修改与风险

本次仅更新知识索引、事故记录和本任务凭证，不改业务代码、数据库结构或生产配置。临时/调试文件、旧报告、`.worktrees` 和生产运行目录中的未跟踪文件均保留，删除前需用户单独确认。

## 下一步

上游 Token 恢复后，重新执行公告增量同步并验收 `stale=false`、质量状态正常；再决定是否清理已列出的工作区和生产目录候选文件。

## 完成证据与知识去向

- 根因、同类范围、回归证据和线上遗留状态：`docs/incidents/INC-0001-下修计数未按公告重置.md`。
- 项目入口和当前事实：`docs/知识索引.md`。
- 部署通用门禁未改写，仍以 `docs/生产部署流程.md` 为准。
- 平台私有 Memory 未写入；项目 `.codex/config.toml` 明确关闭原生 Memory。
