# 存在小站知识索引

> 范围：仅 `portfolio-server`。这是所有 Agent 的项目知识入口，不是历史资料全集。
> 更新：2026-09-02。

## 2026-09-02 退市债固定价格归因修复（本地已实现）

- 根因：华泰账户的搜特退债（`404002`）已退市，行情源按规则不返回报价；虽然持仓保留了最后退市价 `2.362`，净值归因仍因目标日没有 `daily_prices` 把它判为缺失，导致整段归因不完整。
- 修复：`server/services/navAttribution.js` 对代码或名称明确属于退市债的标的，在没有有效日行情时使用持仓/快照/最后成交中保留的正数价格作为固定退市价，并返回 `manualPriceCodes` 留痕；普通证券缺行情仍严格标记不完整。
- 核查：本地华泰账户归因由 `complete=false、missingCodes=[404002]` 修复为 `complete=true、missingCodes=[]、manualPriceCodes=[404002]`，快照残差约为 0；新增退市债归因回归测试。技术口径已同步至 [`技术架构.md`](技术架构.md) 9.1。

## 2026-09-02 场内基金/ETF 行情缺失（已完成生产发布）

- 根因：历史持仓中的 `160719`、`161226`、`511880` 不在 `stock_basic` 统一主档；旧代码把 15/16 开头基金误判为沪市、把 51 开头 ETF 的标准后缀推成 `.SZ`，统一腾讯映射解析失败后页面只显示旧价格和涨跌幅 `--`。腾讯源实测分别返回 `sz160719`、`sz161226`、`sh511880`。
- 修复：统一分类改为沪市 `5xxxxx`、深市 `15/16/17xxxx`、深市 REITs `180101–180999` 与商业不动产 REITs `181001–181999`、深市封闭式基金 `184xxx`；标准代码映射同步校正，20xxxx 深市 B 股、900xxx 沪市 B 股和 92xxxx 北交所股票也纳入同一规则。腾讯行情遇历史基金缺主档时先由统一身份写入口补齐最小主档和腾讯映射，数据库暂不可用才允许本次按同一规则直连。批量与单只入口均已验证返回价格、涨跌幅和行情时间。
- 官方依据已记入 [`技术架构.md`](技术架构.md)：[深交所证券代码区间表（2024 年 4 月修订）](https://www.szse.cn/marketServices/technicalservice/doc/P020240510623040090566.pdf)、[深交所商业不动产 REITs 代码段通知（2026-03-06）](https://investor.szse.cn/marketServices/technicalservice/notice/t20260306_619323.html)、[上交所 ETF 代码段扩展（2026-08-31）](https://www.sse.com.cn/lawandrules/guide/stock/jyglywznylc/zn/c/c_20260831_10830435.shtml)、[上交所 ETF 代码段扩展（2026-07-13）](https://big5.sse.com.cn/site/cht/www.sse.com.cn/lawandrules/guide/stock/jyglywznylc/zn/c/c_20260713_10825354.shtml)、[北交所代码编制指引](https://www.bse.cn/jygl_list/200011019.html)。
- 验收：全量核查当前持仓 6 只场内基金/ETF（160719、160723、161226、501312、510900、511880）全部返回价格、涨跌幅和行情时间；全量测试 112 通过、0 失败、0 跳过；`check:knowledge` 通过。版本 `0.7.0.8`、提交 `4347c77` 已推送并完成生产部署；生产 Web、Worker、健康检查定时器均 `active/enabled`，生产 `/health` 返回 `0.7.0.8`，`cunzaitouzi.cn` 与 `www.cunzaitouzi.cn` 均返回 200 并加载 `code-classify.js?v=5`。本次仅发布代码和文档，未执行生产数据同步。

## 2026-09-02 整改后告警复盘与统一修复

- 早间生产开放告警 10 条实际收敛为约 6 个计划实例问题，根因为预算日键被业务日期污染、Node/Python 跨进程调用计数未延续、任务单次上限与每日预算混用、安全评分早于共享主链，以及同一计划告警未归并；接口权限限制按 Tushare API 区分，不能把单个 API 失败解释为整套 Token 受限。
- 本地已完成修复：预算按上海实际请求日计数；Python 错误结构化返回并由 Node 传递累计调用数；`JOB_BUDGET_EXCEEDED` 不再自动重试；批量任务单次上限与每日计入值分离（每日计入合计仍为 72）；安全评分改为 08:30 并依赖 08:00 主链；同槽位告警归并；下修动机批次边界保留游标并顺延。详见 [`INC-0004-整改后预算与定时计划告警.md`](incidents/INC-0004-整改后预算与定时计划告警.md)。
- 已按授权发布版本 `0.7.0.7`（提交 `2a4ca5d`）并完成生产补跑：安全评分槽位 `397522` 生成快照47，来源日期与分区均为 `2026-09-01`；强赎公告槽位 `383588` 成功发布252条数据。真正开放告警由8条降至3条，剩余为 IPO/套利受预算顺延的历史批次，暂不强行重试；后续按专项方案连续观察3个交易日。

## 2026-09-02 持仓行情缺失与历史证券主档收敛（已完成生产执行）

- 已确认根因：统一身份解析遇到历史裸代码/错误后缀重复主档时判定歧义，腾讯实时行情被静默丢弃，持仓页因此沿用旧价格并显示涨跌幅 `--`；不是腾讯上游无报价，也不是统一取数架构本身冲突。
- 按用户授权完成版本 `0.7.0.6`（提交 `1526551`）生产发布。备份 `/var/backups/portfolio_before_0_7_0_6_suffix_validation_20260902_133700.dump`，SHA-256 `970b05074893686fd720841f6d09a2e9e57fd3e7ed346a2b04fa45111662b4a3`。迁移125/126在单事务内自动归并并物理删除历史主档、迁移外键、清理 `nan/HTML` 无效主档、收敛公司关系、清空候选记录并校验后缀冲突，不需要人工逐条处理；生产重复标准身份、候选、无效代码、非标准市场代码、重复发行公司关系均为0。
- 发现的7组北交所错误后缀（包括 `.SH.SH`、误标 `.SH/.SZ`）已统一为 `.BJ`；数据库唯一索引已建立。`920002.SH→920002.BJ`、`600519.BJ→600519.SH` 等校验通过。生产 Web、Worker、健康检查定时器均 `active/enabled`，健康版本 `0.7.0.6`。统一批量行情抽查 `601919`、`601198`、`123175` 均返回价格、涨跌幅和行情时间；持仓数量与交易数量未被迁移改写（当前分别为72、98）。
- 全量测试最终结果为 112 通过、0 失败、0 跳过。后续仅需按 incident 文档连续3个交易日观察行情完整率、批量请求量、净值快照和告警，不再执行人工主档处理。

## 2026-09-01 证券数据架构、自动任务与 API 调用保护专项整改方案（首批已落地，分批执行中）

- 本轮讨论已统一整理进既有专项文档：[`自动任务重复执行与API调用保护整改方案_20260815.md`](自动任务重复执行与API调用保护整改方案_20260815.md)，没有另建重复方案。原第 1～10 节保留 2026-08-15 初版背景，第 11～22 节为当前整改依据；内容冲突时以后续章节为准。
- 当前专项范围已经扩展为：统一公司/证券身份和多数据源代码映射、股票基础资料与行情及财务三表标准层、可转债按 `stock_instrument_id` 关联正股、6,000 积分 Tushare 批量采集、共享采集任务、平滑限速、接口级熔断、主备 Token 隔离和数据分区发布。
- 评审后已明确区分“采集止血”和“数据架构治理”：现有23个任务定义（20个定时、3个人工）的任务契约和基础调度能力不重复开发；常规自动 Tushare 调用目标不超过80次/交易日、硬上限100次，核心主档、收盘和停牌分区不超过10次；`ops.dataset_partitions` 前置到止血批次，历史证券ID合并推迟到最后。
- Python Guard 开启后已与 Node 共用预算表和数据库锁，整改重点是统一两端策略语义并禁止生产脚本无保护运行，而不是再分配一套独立额度；证券身份与权限探测分别改造现有 `securityIdentity.js`、`externalApiConfig.js`，禁止新增平行实现。
- 2026-09-01 补强四项必做：运行时所有供应商代码转换强制读取 `instrument_identifiers` 并使用有上限的内存缓存；Python Guard 改为默认开启；`suspend_d` 从估值计算移入 P1 共享采集；任务/接口/数据集/预算矩阵从 `JOB_DEFINITIONS` 自动生成并由 `check:knowledge` 校验。
- 本轮已执行 P0/P1/P2/P3 的可逆部分：Node/Python 外部预算语义统一为窗口等待（`BUDGET_WAIT`），预算扣减统一调用 `ops.consume_external_call_budget()` 数据库原子函数，Python Guard 默认开启且生产不可关闭；`suspend_d` 从估值链路移入可转债主采集链路；共享收盘批次用一次 `stock_basic` 建立全A股标准主档，并一次写入 `daily`、`daily_basic`、`adj_factor` 全市场分区；运行时供应商代码解析接入 Node/Python 两套身份服务的有界缓存，腾讯/东方财富/新浪/雪球代码缺失时由身份模块先落映射并校验，业务层不再手工拼接；并为指数补齐标准身份和映射。迁移119～124已完成股息率单位修复、分区水位初始化、历史 `equity`→`stock` 归一和公司关系补齐。任务矩阵由 [`任务接口数据集矩阵.generated.md`](任务接口数据集矩阵.generated.md) 自动生成，`check:knowledge` 会校验是否漂移。新增 [`server/scripts/acceptanceDataArchitecture.js`](../server/scripts/acceptanceDataArchitecture.js) 与 `npm.cmd run check:data-architecture` 提供不调用外部接口的只读验收。新增迁移118和 [`server/scripts/auditInstrumentMerges.js`](../server/scripts/auditInstrumentMerges.js) 已完成只读审计/候选留痕：本地发现4,538组候选（4,367组名称一致、171组需人工核对、当前外键引用4,810行；审计已排除候选表自身的记录），没有执行物理合并或删除。Tushare 权限探测入口为 [`server/scripts/probeTusharePermissions.js`](../server/scripts/probeTusharePermissions.js)，已扩展 `income_vip`、`balancesheet_vip`、`cashflow_vip`、`fina_indicator_vip`，探测本身受统一预算和熔断保护。自动验收：全量测试111通过、0失败、0跳过；本地最新估值股息率异常0条、已发布分区空分区0条、有效行情转债缺正股关联0条、股票 `equity` 0条。仍有6条历史股票主档无名称无法补公司关系、4,686条历史裸股票代码（139条可直接规范化、4,547条需合并）及185条旧测试熔断记录；生产切换、历史重建和旧ID物理合并仍需按专项分阶段授权。
- 2026-09-01 已按授权完成生产切换与维护窗口可逆处理：生产提交 `c989bfb`、版本 `0.7.0.0`，部署前备份 `/var/backups/portfolio_before_0_7_0_0_20260901_191425.dump`（SHA-256 `ef153e23cf879f5d803c4923e2e428553b626b2c56849b614357e91932d38836`）。Tushare 主/备 Token 权限探测 48 项中 18 项可用或空结果可接受；主 Token 24 项均未发出上游请求（多数被统一 Guard 当日预算保护拦截），备用 Token 4 个 `_vip` 接口无权限，另有 2 个接口受限。生产已规范化 139 条裸股票代码，并生成 4,789 组历史 ID 合并候选（4,624 组名称一致、165 组需人工核对、9,350 行引用）；未做物理合并/删除，生产旧 `test_guard` 熔断记录为 0。影子对账从下一交易日开始，须连续 3 个交易日核验；部署前最后一次共享收盘任务为 281 次 Tushare 调用，观察期未完成前维持 `pass_with_controlled_pending`。
- 2026-09-01 版本 `0.7.0.1`（提交 `a56eb12`）按授权完成生产维护窗口：备份 `/var/backups/portfolio_before_id_partition_0_7_0_1_20260901_201106.dump`（SHA-256 `e50c32ea9f3856be91d0807e2410d3bd0cc95c32303eda8319ea08c2ac3ad72d`）。历史 ID 安全组迁移 4,537 个旧主档、更新 217 条外键引用、去重 4,537 条确定重复公司关系；旧主档保留并标记 `merged`，迁移后旧 ID 外键引用 0。剩余 169 条候选待处理（165 条人工冲突、4 条无明确标准目标）。17 个非核心数据集已回填并发布分区，生产共 23/23 个任务产出数据集有分区、空发布 0；Web、Worker、健康检查定时器均 active，健康版本 `0.7.0.1`。影子对账仍需连续 3 个交易日观察。
- 已建立后续观察安排：从 2026-09-02 起按 `market.trade_calendar` 识别上海交易日，连续 3 个交易日记录共享采集与部署前 281 次 Tushare 调用基线的差异；主 Token 权限探测仅在首个符合条件的自然日执行一次，固定走 Guard 和数据库预算，不切备用、不绕过预算、不循环重试。
- 2026-09-02 首个观察窗口已留痕：生产当前健康版本为 `0.7.0.4`（提交 `4fed117`，后续外部发布版本）。共享任务最近两次运行分别为 `job_runs.id=1943` 成功且外部调用 0 次、`job_runs.id=1933` 失败且 Tushare 调用 8 次，均低于 281 次基线。主 Token 权限探测 24/24 接口均在 Guard 预检阶段被当日预算 `100/100` 拦截，未发起上游请求、未切备用、未重试，权限结论保持待定；`cb_price_chg` 既有熔断为 `PERMISSION_DENIED` 且无探测租约残留。因 `market.trade_calendar` 尚无 2026-09-02 记录，本日不计入连续 3 个已确认交易日，观察期继续保持 `pass_with_controlled_pending`。
- IPO链路已按统一任务架构收敛（业务实现版本 `0.7.0.2`、提交 `54fdf89`）并完成生产部署：保留 `ipo_history_sync` 任务代码，18:00作为 `new_share` 唯一采集者并发布带集合/批次/缺失诊断的 `ipo_history` 当天分区，19:30仅补非紧急字段；`ipo_calendar_refresh` 调整为18:05，依赖当天质量通过的已发布分区，日报只读标准事实并在发布前做证券集合对账。不一致时保留上一份有效日报并告警。分阶段预算为核心1次、晚间补全15次、日报0次，任务矩阵每日声明总额72次；隔离测试库全量测试111通过、0失败、0跳过，生产 Web、Worker、健康检查定时器均 `active/enabled`，代码部署验收时健康版本和变更记录为 `0.7.0.2`，IPO日历/历史接口返回200；本次 `0.7.0.3` 仅同步发布元数据，不改业务代码。部署当日 Tushare 自动任务已达到100次硬上限，18:00核心阶段处于 `waiting_external`、18:05日报按门禁 `blocked` 并保留旧结果，19:30非紧急补全成功；未执行人工生产补数。

## 2026-08-31 下修动机详情可读性与正股财务缓存修复（本地已实现）

- 详情页“查看计算项”默认展开；前十大持有人、控制人不再直接显示原始 JSON，改为中文明细；历史下修按“第1轮、第2轮……”逐轮换行；移除“市场每月提议下修次数”输入项，市场热度仍只作为背景，不进入单券评分。
- 正股财务取数补齐 `bond_safety_financial_cache` 的实际字段映射（`cash`、`total_liability` 等），并在详情审计来源中标注为“已入库正股财务缓存”。本地 2026-08-28 候选核查：309 只中 290 只有财务缓存，290 只可读到账上现金和负债，228 只有资产合计；其余没有可用缓存的正股仍需后续数据补齐，页面明确显示暂无数据。

## 2026-08-31 下修动机研究等级与详情入口修复（本地已完成）

- 下修列表的“动机等级”不再因 `unavailable`、`partial` 或已发生下修事实而取消链接；当前列表中的每只转债都能进入下修博弈详情，没有评分快照时详情页也会明确说明等待计算。
- 回测未通过仍禁止输出“强动机/存在动机”等预测结论；列表改为按同一评分日、同一模型的全市场分位展示非预测性的“研究等级较高/中等/较低”，其中前20%为较高。本地重算 309/309 成功、0失败，分布为较高64只、中等149只、较低96只；数据质量为73只 `complete`、236只 `partial`。
- 修复数据质量误判：Tushare `top10_cb_holders` 不提供公告日，历史取数改用年报4月30日、中报8月31日等法定最晚披露日作为保守可见日；`pledge_stat` 正常空结果使用成功游标标记为“已核验未返回记录”，不再与接口失败混淆。
- 修复回测收益口径：上线门槛中的扣成本收益只统计高分组，不再误用全部样本平均收益。修复后重跑仍为364个样本、156个样本外样本、0个70分以上样本、关键输入完整率0%，高分组收益因此为无样本而非原先的全样本负收益；`motive-v1.1` 预测门禁保持关闭。

## 2026-08-31 下修动机评分生产发布与验收（已完成）

- 版本 `0.6.5.7` 的代码发布提交 `61795e6` 已推送 `master`，并按标准生产脚本完成部署；部署时自动执行迁移 115，`analytics.convertible_bond_revision_motive_backtests` 已在生产存在。
- 生产 Web、Worker、健康检查定时器均为 `active/enabled`；健康检查版本为 `0.6.5.7`，就绪检查中的数据库与 Redis 均通过。下修接口返回 `stale=false`、公告质量正常，当前 309 只候选中 5 只已有下修事实、0 只被标为 `observed_weak`，309 只动机等级为 `unavailable`，符合回测未达标时的安全策略。
- 生产页面资源版本为下修列表 `45`、动机详情 `54`；该条是版本 `0.6.5.7` 的历史验收记录，后续本地修复的目标资源版本为下修列表 `46`、动机详情 `55`，未部署前不得视为线上现役。
- 本次仅发布代码与数据库迁移，未执行生产历史回填或主动输入同步。部署后调度器重试了 2026-08-29 的既有动机输入同步计划，该计划因 Tushare `top10_cb_holders` 及备用接口失败而保持 `degraded`，属于数据源/额度问题，不是本次发布失败；后续补数需单独授权。

## 2026-08-31 下修动机历史回填与回测验收（本地已执行，模型保持关闭）

- 新增迁移 115 与 `analytics.convertible_bond_revision_motive_backtests`，回测入口为 `server/scripts/backtestConvertibleBondMotive.js`；支持公告前时点取数、2025 年样本外窗口、0.30% 往返成本和历史评分快照幂等回填。
- 本地已回填 364 个历史触发周期评分，覆盖 2022-08-01 至 2025-12-31；样本外 156 个周期、34 个提议下修，关键输入完整率 0%，70 分以上组 0 个，扣成本平均收益 -0.1943%，回测结果明确未达上线门槛。
- 因回测未达标，`motive-v1.1` 继续输出研究分但统一为 `motive_level=unavailable`；已提议/已实施等只显示公告事实，不再显示“动机偏弱”预测。持有人同步已成功写入部分历史事实（本地63只、4,730条），每批最多10次；正常空结果推进游标，真正权限错误保留失败游标，达到调用上限或接口停止后未实际请求的对象不推进游标，不以空数据冒充完整。
- 证据：`server/test/convertible-bond-motive-backtest.test.js`、`npm.cmd run backtest:motive`（结果 pass=false 为预期安全门禁）、`npm.cmd run test:all`、`npm.cmd run check:knowledge`。生产发布和生产数据同步仍需按生产流程取得授权。

## 2026-08-31 下修动机生产补数与空结果排序（已发布）

- 版本 `0.6.5.5`、提交 `f2f111c` 已发布生产：同步候选继续按当前行情过滤；已尝试但为空的持有人/质押对象按 `last_attempt_at` 后置，避免批次反复卡在同一批，同时保留后续复查机会。
- 已获用户授权并完成 2026-08-28 生产输入补数：当前 309 只候选中 258 只具备 `top10_cb_holders` 持有人事实（全库 276 只、17,200 行）；`pledge_stat` 共 2 家、均为 2026-08-28 快照，分别对应 000001.SZ、600690.SH，不属于当前 309 只候选。其余 51 只经过多轮正常空结果复查，未覆盖对象以 Tushare 空数据留痕，不覆盖旧事实。
- 309/309 只已重算 `motive-v1` 评分、0 计算失败；当前 `quality_status=partial`、完整率 0.666667，表示部分上游输入缺失，不表示评分任务失败。生产 `/api/bond-revision` 已返回 `stale=false`、`quality.status=ok`；Web、Worker、健康检查定时器均 active/enabled。
- Tushare `top10_cb_holders` 的 30 次/分钟限频由接口级熔断和每批最多 10 只保护；备用 Token 对该接口无权限，本次补数强制主 Token 后恢复自动模式。后续由 Worker 按 72 小时周末补偿调度继续复查。

## 2026-08-31 下修动机评分止血修正（本地已实现，回测未达标）

- `motive-v1.1` 删除“剩余规模低于发行规模”的无意义加分；“剩余规模/发行规模”仅保留为未转股比例，在达到70%或90%时计入促转股压力。
- 全市场及同行其他转债的下修提议不再计入单债动机分，仅作为详情页市场背景，避免把没有可靠因果说明的相关性当成发行人动机。
- `partial` 输入或历史样本外回测未通过时仍保存原始分数、明细和来源，但列表与详情不再输出“动机偏弱/存在动机”等预测等级；已提议、待表决、已通过、已实施、已终止状态显示公告事实。完成回测后必须升级模型版本并显式打开校准开关。
- 下修列表只读取当前 `motive-v1.1` 快照，旧 `motive-v1` 结果保留追溯但不再混入当前视图；本地 2026-08-28 评分需重新计算后生效。
- 当前本地库基线：309只当前候选全部 `partial + unavailable`；历史评分已扩展到292个交易日，但关键输入完整率仍不足95%，说明模型未达到预测上线门槛。
- 回归证据：`server/test/convertible-bond-motive.test.js`、`server/test/convertible-bond-revision-frontend.test.js`、`server/test/convertible-bond-motive-backtest.test.js`；生产输入同步和生产发布仍需按生产流程单独授权。

## 2026-08-30 可转债下修动机评分代码链路已实现

- 按《可转债下修动机评分功能_研发方案.md》落地迁移 111：持有人持仓、持债变动、公司质押、下修周期和每日动机评分表，均支持幂等写入及评分输入快照/来源/哈希留痕。
- 新增 `convertibleBondRevisionMotiveService`，实现 `motive-v1` 五维评分、触发成熟度、可执行性、风险单列、解释项和质量状态；研究评分≥70暂显示“待历史校准”，未把评分当成确定性下修结论。
- 现有 `/api/bond-revision` 增加动机等级，新增 `/api/bond-revision/:tsCode/motive-detail` 和 `/bond-revision-motive.html?code=...`；页面只读数据库评分快照，不调用外部接口。
- 新增收盘日次日 07:20 的 `convertible_bond_revision_motive_inputs_sync`，08:15 与现有下修数学进度串行计算。未执行生产历史回填或生产同步，需用户按生产流程单独授权。
- 证据：`server/test/convertible-bond-motive.test.js`、`server/test/convertible-bond-revision-frontend.test.js`、`npm.cmd run test:all`、`npm.cmd run check:knowledge`。

## 2026-08-30 下修动机评分验收修复（本地已闭环）

- 修复评分主链实际 SQL 错误：转股价值字段补入查询，候选范围改为当前交易日行情、公开上市债券、生命周期、条款和强赎停止日的统一口径；真实本地库验收为 310/310 只评分、0 失败、结果全部落库。
- 迁移 112/113/114 补齐评分范围约束、持有人变动前后值/清仓标记、质押统计字段、触发周期首个匹配日/结束日/标签和治理检索索引；连续触发观察不再机械按 240 天拆周期。
- 修复历史时点隔离：财报、持有人、质押按公告可见日读取，正股 PB 与收盘价要求同一交易日；PostgreSQL `DATE` 按上海日历日转换，详情指定 `tradeDate` 不再偏移一天。
- 修复 Tushare 契约和单位：`top10_cb_holders` 只请求支持字段并将 `hold_amount` 万张换算为张；`pledge_stat` 只请求支持字段并将股份数量万股换算为股；同步任务增加 `ops.raw_records` 和 `ops.sync_cursors` 留痕，外部调用不包在数据库事务内。
- 详情页现在展示维度中文名、核心动机、阻断原因、指标原始值、单位、规则、变化和数据日期，不再展示来源引用或完整输入 JSON；页面仍只读评分快照，不调用外部接口，接口内部保留评分快照和来源留痕供审计。
- 修复列表动机等级全部显示“暂无数据”：`partial` 质量状态只表示部分输入缺失，仍保留已有评分并显示 `weak`/`has_motive`/`research_high`；只有 `incomplete` 才显示“暂无数据”。本地重新计算 310 只候选后，列表接口已返回实际等级，前端缓存版本更新为 44。
- 下修动机详情数值展示已统一：历史价格分位转为百分比并保留两位小数，距离到期年数保留两位小数，布尔值和状态值使用中文；详情资源缓存版本更新为 48。
- 规模比例计算项“剩余规模/发行规模”和“剩余规模/正股市值”已按百分比展示并保留两位小数，不再显示为小数倍数。
- 输入数据区的正股 20 日成交额加权均价保留两位小数，正股总市值、剩余规模和发行规模统一换算为亿元并保留两位小数。
- 输入数据区的财务指标、历史下修情况和市场每月提议下修次数已改为中文大白话说明，不再直接展示英文键名、JSON 结构或难以阅读的数组；详情资源缓存版本更新为 51。
- 验收证据：`server/test/convertible-bond-motive.test.js`、`server/test/convertible-bond-motive-frontend.test.js`、`server/test/convertible-bond-motive-db.test.js`；本地 PostgreSQL 真实评分回归通过。历史全量回填、Tushare 生产同步和生产发布仍未执行，需单独授权。

## 2026-08-30 强赎公告漏同步：补录并改造为「收盘日次日」调度（已修复并固化）

- 现象：欧通转债（123241）8-28 盘后发布「不提前赎回」公告，生产强赎监控页仍显示「已满足待确认」。
- 根因不是熔断也不是关键词：强赎公告同步原为 `weekdays: true`，只在**交易日当天** 07:45 跑一次，收盘后不补跑、周末不生成计划槽位，周五盘后的公告要拖到下周一才入库。8-28 09:09 那次跑成功（320 条）时公告尚未挂网。
- 关键词假设已证伪：默认 13 个关键词能搜到该公告（`提前赎回` 命中），`classifyCallEvent` 也能正确分类为 `waive`，PDF 解析出 `no_call_until=2026-11-28`，**同步链路代码无需修改**。
- 生产已补录并验收：备份 `/var/backups/portfolio_before_ot_ann_backfill_20260830_155238.dump`；欧通 `business_status` 由 `met_pending` → `waived`；同类核查又补上应流转债赎回结果、恒逸转2（最后交易日 2026-09-04 / 最后转股日 2026-09-09）、鸿路转债（2026-09-28 / 2026-10-08）。接口 `stale=false`、已满足待确认 1 → 0。
- **调度已根治**：新增 `afterTradingDay` 口径（`jobDefinitions.js` 声明 + `jobScheduleSlots.js` 的 `isSlotDayAllowed()`），语义是「收盘日的次日」即“前一天是交易日”。普通周落在周二至周六，节假日落在假期第一天，假期其余日期与假期后的周一都不执行。判定复用 `isWeekday`，已内置节假日数据，无需另查交易日历。适用任务：可转债安全性、下修与转股价公告事实、强赎公告、可转债行情同步、可转债估值预警；收盘后跑的任务（收盘价、净值、指数、打新日报等）维持 `weekdays` 不变。回归测试 `server/test/job-slot-after-trading-day.test.js`。
- 固化工具：`deploy/backfill_bond_call_announcements.py`（`--audit` 只读核查、`--sync --confirm-production` 备份＋补录＋验证），用法见 `docs/生产部署流程.md`。
- 已知既有问题（与本次改动无关，已用 `git stash` 验证改动前同样失败）：`server/test/tushare-routing-regression.test.js` 的熔断并发探测断言稳定失败，全量测试当前 105 通过 / 1 失败。

## 2026-08-30 可转债估值历史完整性断档（已修复）

- 原基线为行情 886 天、估值 880 天，末尾待估值 4 个交易日；核查确认 6 个缺口分别为 2 个内部断档和 4 个末尾缺口，不是行情源缺失。
- 根因是估值日常刷新只按 `ops.sync_cursors` 游标之后补偿；游标已越过内部断档时，旧逻辑不会重新发现这些日期。
- 已用现有历史回填入口补齐 2026-08-20 至 2026-08-28 的 7 个交易日窗口，当前行情/估值均为 886 天，最新日均为 2026-08-28，缺口为 0，游标已到 2026-08-28。
- `server/scripts/convertibleBondValuation.py` 已改为刷新时直接检查行情日与估值日差集；回填游标使用单调推进，补旧断档不会倒退。估值专项回归 54/54，全量测试 105/105 通过。
- 已获授权发布并同步线上：生产版本 `0.6.4.39`、提交 `79c761759d7f222a9163582e9630465668eb15c2`；线上回填 7 个交易日、1,971 只正式估值。按自 2021-01-01 的完整口径复核，行情/估值均为 1,371 天、内部缺口 0、末尾待估值 0，游标为 2026-08-28 且无错误；Web、Worker、健康检查定时器均 active，估值接口 `stale=false`。
- 详细复盘见 `docs/incidents/INC-0002-可转债估值历史完整性断档.md`。

## 2026-08-30 后台告警与外部熔断探测卡死整改（已闭环）

- 生产核查确认当前不是大量不同告警，而是 6 条仍开放记录：5 条与同一批两个历史失败计划直接相关，另有 1 条 `worker:overdue-slots` 汇总记录因这两个失败计划持续存在而重复计数。
- 两个失败计划均发生在 2026-08-28：可转债公告历史同步因 `stock-analysis` 当日额度耗尽失败；套利同步因 `cninfo` 限流熔断失败。`cninfo` 的恢复时间已过，但 `probe_in_flight` 仍为 `true`，说明 Worker 在探测期间退出后占用未释放。
- `server/services/externalCallGuard.js` 已改为“探测所有者＋一次性令牌＋5分钟租约”，异常退出后可安全回收；限流/额度耗尽的任务进入 `waiting_external`，不消耗业务重试次数；套利任务按港交所与巨潮资讯两者的最慢游标验收，可转债公告任务按公告游标自动恢复。
- Worker 心跳已固定为“主机＋角色”主记录：正常退出标记 `stopped`，异常退出按心跳判定 `stale`，健康检查定期清理超过 7 天的陈旧记录，后台概览只展示每个角色的最新记录。
- 版本 `0.6.4.38` 已完成本地代码验证并发布生产；CNInfo 游标已从已核实的 2026-08-24 自动补齐至 2026-08-30，套利补偿成功，目标活动告警已收敛为 0。Worker 当前按“主机＋角色”只保留 1 条运行记录，历史进程记录为 `stopped` 并按 7 天规则清理。

## 2026-08-30 下修监控两类漏判补强（已部署）

- 根因已确认：太能（`127108.SZ`）条款写的是“价格不低于最近一期经审计的每股净资产”，旧识别只覆盖“不得/不应低于”；斯达（`113702.SH`）触发后没有次一交易日“修正/不修正”事实，旧计算没有按上交所规则隐含重启。
- 已补充存量条款重解析迁移 `109_convertible_bond_revision_net_asset_floor_phrase`，并把上交所触发日后无次日响应统一按次一交易日重新起算；正式事件存在时不触发隐含重启。
- 本地 `2026-08-28` 全量 310/310 重算：太能为 `floor_blocked`，斯达从 `2026-08-07` 起算、已满足 6/15；同类核查当前 `floor_blocked` 共 14 只，未混入 `met_pending` 或 `near`。
- 生产已部署版本 `0.6.4.32`（提交 `430a710`）：迁移 109 已落库，`/api/bond-revision` 返回 `stale=false`、质量检查 0 待解析/0 公告错误；太能为 `floor_blocked`，斯达按重新起算后的窗口累计为 `met_pending`、20/15。
- 随后发现部署后启动补算只校验日期、不校验计算逻辑版本，已执行生产重算使斯达恢复为 `tracking`、6/15；版本 `0.6.4.40` 增加逻辑版本校验，后续规则变更会自动补算。

## 2026-08-29 发布与线上数据同步收尾

- 下修监控修复已由提交 `8fafd1cd7bf516f3b0d6a1b42cfcd8a3e8a23503` 发布，公告取源补强已由提交 `dd09ddd8ab9184de2df458ce2ba323a889d5c4f1` 发布；本次季度报告董事会锁定规则发布前线上版本为 `0.6.4.30`，本次目标版本为 `0.6.4.31`。
- 2026-08-29 线上按用户授权执行一次缓存公告重解析和下修派生重算：缓存候选 312/312 完成、7 条产生解析变化、0 条失败；2026-08-28 派生结果 310/310 完整。备份为 `/var/backups/portfolio_cb_before_reparse_20260829_214304.dump`。
- 随后按用户授权对线上多出的 7 只（`118069、123138、123142、123262、123266、127044、127098`）执行交易所官方历史公告定点补录/补解析，窗口为 `2026-04-01` 至 `2026-08-29`：7/7 成功、7/7 产生数据变化、0 条失败；备份为 `/var/backups/portfolio_cb_before_history_backfill_20260829_214936.dump`。
- 随后根据本地已落库的官方 PDF 记录，对 `123266`（2026-04-10）和 `127098`（2026-08-14）执行 v7 定点补解析并写入线上，解析结果分别锁定至 `2026-10-10`、`2026-09-13`；再次重算 310/310 完整。线上“已满足待公告”最终为 3 只（`113702、123138、127108`），其中 `123138` 的 2026-08-29 新公告覆盖了本地旧快照，故线上不会与本地 2 只完全相同。
- 本次 `0.6.4.31` 发布补强季度报告董事会前无固定日期锁定：`123138` 的公告会识别为“锁定至 2026 年第三季度报告董事会”，核查起点为 `2026-11-01`；正式发布后由每日 07:40 任务核验真实公告并回写日期。
- 事故根因、同类核查、生产补算证据与遗留风险见 `docs/incidents/INC-0001-下修计数未按公告重置.md`；本次收尾凭证见 `docs/tasks/closed/20260829-可转债下修监控发布收尾.md`。

## 2026-08-30 持仓-交易智能识别港股代码修复

- 根因：大模型可能把港股五位代码补成六位数字；统一分类层按“六位=A股”处理，历史名称特例只覆盖深圳国际，导致江西铜业股份 `00358` 可能被识别为 A 股。
- 修复：图片识别、Excel 直解析和 Excel 大模型解析统一查询 `core.instruments`；六位代码与港股五位主档/名称唯一匹配且不存在 A 股同码时，恢复五位代码、港股分类和 HKD。账本写入再做一次同样的主档校正。
- 核查：本地主档确认 `00358.HK=江西铜业股份`、`00152.HK=深圳国际`；本地持仓/交易中的 `00358` 均已是港股分类；回归测试覆盖江西铜业修正和 A 股同码保护。

## 先读什么

每个任务先阅读：

1. `AGENTS.md`：强制规则与交付标准。
2. 本文件：按任务选择权威资料。
3. `governance/knowledge-map.json`：按任务描述和实际改动文件匹配必读、必更新资料与检查。

不要默认全量读取文档。根因不明、跨模块、数据、权限、安全、任务调度或部署变更时，必须扩大阅读范围。

## 权威资料

| 主题 | 权威位置 | 何时读取/更新 |
|---|---|---|
| 技术、数据、接口、任务、配置现状 | `docs/技术架构.md` | 相关开发前阅读；现状变化时就地更新 |
| 可转债功能页视觉规范 | `docs/技术架构.md` 第 5.2 节、`public/shared/style.css` | 新增或调整强赎、下修、安全性等可转债页面时必须复用 `.bond-feature-*` 规范 |
| 生产部署、回滚、验收 | `docs/生产部署流程.md` | 部署或环境变更前阅读；流程变化时更新 |
| 用户可见版本历史 | `CHANGELOG.md`、`public/changelog.json` | 发版或用户可见变化时更新 |
| 重要长期技术取舍 | `docs/decisions/README.md` | 新增不可逆或长期影响的选择时记录 ADR |
| 重大或重复故障 | `docs/incidents/README.md` | 根因、同类范围或防复发需要沉淀时记录 Incident |
| 长任务交接 | `docs/tasks/README.md` | 跨会话、跨 Agent 或未完成工作时维护任务卡 |

## 统一工作流

开始：确认项目根、Git 基线和未提交改动；按路由读取资料；记录不确定点。

诊断：从数据源、数据库、服务、接口、页面和运行环境检查完整链路；区分直接原因、根本原因与保护机制失效原因；检查同类对象和同类功能。

收尾：用实际 `git diff --name-only` 再跑一次路由；更新命中文档；重要取舍写 ADR，重大或重复问题写 Incident；运行 `npm.cmd run check:knowledge`。

## 平台约定

Git 仓库中的本索引、`AGENTS.md` 和路由表是唯一项目事实来源。平台私有 Memory 只能辅助召回，不得写入或裁决项目事实。

| 平台 | 状态 | 接入方式 |
|---|---|---|
| Codex | 已接入（2026-07-27 起） | 工作区根 `AGENTS.md` 入口 + 项目 `.codex/config.toml` 关闭自带记忆 |
| WorkBuddy | 已接入（2026-08-29 起） | 工作区根 `AGENTS.md` 自动注入；私有记忆只做指针，规则见根目录 `AGENTS.md` 的"多 Agent 协作：记忆路径统一规则" |

机制说明：WorkBuddy 每次会话自动读取工作区根目录 `AGENTS.md`（不读 `portfolio-server/AGENTS.md`），因此路由靠根目录入口文件转发。它没有类似 Codex 的记忆开关配置项，只能靠规则约束，不要臆造配置。

未验证的平台不得宣称已接入，也不得复制本项目的事实到私有记忆。
