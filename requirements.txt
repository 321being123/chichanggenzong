# Python 依赖（单一权威来源，覆盖 server 与 ipo-report 全部脚本）
# 安装：pip install -r requirements.txt
# 如需按实际环境重新生成精确版本，可在已装好依赖的 venv 中执行：
#   pip freeze | grep -E "psycopg2-binary|tushare|pandas|numpy|requests|PyMuPDF|xgboost|akshare|paramiko" > requirements.txt
psycopg2-binary==2.9.9
tushare==1.4.6
pandas==2.2.2
numpy==1.26.4
requests==2.32.3
PyMuPDF==1.24.10
xgboost==2.1.1
paramiko==5.0.0
akshare==1.18.64
