# INC-0026：后台补档任务阻塞与完成判定失真

状态：版本 `0.8.1.39` 代码已部署，本地与线上运行验收通过；生产恢复与连续观察待授权
日期：2026-09-16

## 现象与影响

后台“可转债行情同步”在 2026-09-16 08:00 失败，提示“最近30个交易日仍有853只正股未完成行情补水（本次上限80）”。当前在市可转债只有约 310 只，错误数量明显超过真实业务范围。

同类核查进一步发现：公司财务正常空跑被严格分区判失败、港股 IPO 历史陈旧记录长期占队、套利待解析被水位改成成功、市场波动调用上限和子数据集新鲜度不匹配。

## 根因链路

计划实例目标日 → 业务子阶段自行按当前日计算窗口 → 历史档案和陈旧上市状态进入候选集合 → 固定批次处理后抛错 → 下一次尝试继承累计外部调用数 → Guard 提前阻断 → 分区或水位再次参与完成判定 → 后台持续失败或显示假成功。

生产只读核查确认：补水集合为 936 只正股，首次存在缺口 933 只；单批处理 80 只后剩余 853 只。当前真实在市可转债约 310 只，且 729 条历史转债档案的 `source_updated_at` 为空。

## 直接原因、根本原因与保护机制失效原因

- 直接原因：最近 30 日窗口没有绑定任务目标交易日；正股集合主要依赖历史档案和陈旧 `listed` 状态；固定批次未建成可续跑状态。
- 根本原因：目标日期、对象集合、单次预算、槽位累计量、阶段进度和最终完成判定没有清晰分层。
- 保护机制失效：`maxExternalCallsPerRun` 实际跨重试累计；若简单改成每次归零又缺少槽位总止损，会反向放大调用；Runner 未完成可被水位恢复覆盖；严格数据集发布不能识别已核验无变化。

## 同类范围与核查结果

- 当前存在：可转债行情同步、公司财务增量、港股 IPO 补全、套利公告同步、市场波动指标。
- 历史同类但当前未复现：港币汇率。
- 已有针对性修复且本次不重复改造：A 股 IPO 补全。
- 本次未发现同类阻塞：A 股/港股/转债/基金收盘、指数、净值、港股每手股数和港股交易日历。

完整证据和逐任务方案见[专项整改研发方案](../后台补档任务阻塞与完成判定失真专项整改研发方案_20260916.md)。

## 修复与回归证据

本轮已完成代码整改、本地回归和标准生产代码部署；未修改生产数据库、生产任务或历史数据，因此生产恢复结果和连续观察仍不能提前宣称完成。

已落地：续批状态与 `next_attempt_at`、尝试/槽位调用计数分离及总止损、可转债目标日与阶段续跑、财务 `verified_no_change` 分区、共享恢复证据服务、套利 PDF 未到期/耗尽分流、市场波动子数据集新鲜度诊断、港股 IPO `pending_not_due` 候选状态及后台详情展示。

本地证据：`npm.cmd run test:all`（测试库 portfolio_test：通过 132、失败 0、跳过 0）与 `npm.cmd run check:knowledge`（知识治理检查通过，命中 4 条路由）；生产恢复和连续 3 个交易日观察仍需单独授权。

关闭前必须补齐：实际提交、应用版本、全量测试结果、知识门禁、任务运行编号、对象集合对比、各子数据集日期及连续 3 个交易日观察结果。

## 防复发措施与遗留风险

计划修复包括：目标日贯穿、目标日有效对象集合、单次/累计调用数分离、槽位总止损与无进展保护、正常续批、已核验无变化分区、中立恢复证据优先于水位、陈旧候选终态及子数据集独立新鲜度。

遗留风险：在整改上线前，人工重复补跑仍可能继续消耗外部调用或形成误导状态；不得通过直接改槽位成功、简单调大上限或删除历史对象临时绕过。

## 补录（2026-09-16）：第三轮复核定稿

本方案的根因分析成立。第三轮复核进一步对照当前代码，修正了第二轮补录中的三项误判，并把未闭合的续批、预算和终态语义定稿；完整口径见[专项整改研发方案 §12](../后台补档任务阻塞与完成判定失真专项整改研发方案_20260916.md)。

### 第三轮确认的关键风险与裁决

1. **计数与止损（2026-09-17 复核修正）**：调用数继续按尝试和槽位真实累计，但未知的单批、槽位和每日调用上限不得自行填数；任务改用超时、连续无进展、持久化剩余进度和最长续跑时间止损。→ 方案 §12.1
2. **公司财务核验分区**：采用带证据的核验快照分区，不伪造新增财报，也不把已有事实表写成零行；分区保留真实事实总行数和最近真实披露日，diagnostics 记录本次变化为 0。→ 方案 §12.2
3. **恢复证据层级**：`verifyAlertScope` 是告警关闭判定器，不能直接决定当前失败槽位转成功；须抽取唯一中立的运行、阶段、分区和水位证据服务，由调度器和告警层共同调用。→ 方案 §12.3
4. **目标日已有入口**：`jobRunners.js` 已计算并传入 `targetTradeDate`，真正缺口是生命周期筛选、周期补漏和正股补水子阶段丢失参数；不新增第二个日期入口。→ 方案 §12.6

### 遗漏补齐

5. **续批状态与停止线**：`partial` 只作为结果/展示态；新增 `continueSlot()`，正常续批把领取时增加的 `attempt_count` 减回，`pending` 必须遵守 `next_attempt_at`，并只执行 `pendingStages`。→ 方案 §12.4
6. **预算权威文档漂移**：第三轮复核前旧专项仍把“80 次/交易日”写成现行要求，但当前生成矩阵为 680/日且知识门禁只校验矩阵一致性；本轮已同步裁定 80 次为历史优化目标，不是当前硬门禁，实际请求继续按运行、槽位和来源三层对账。→ 方案 §12.5
7. **终态与新鲜度缺口**：港股 IPO 候选需读取 `terminal_missing/next_retry_at`；套利 PDF 的 `not_due/exhausted` 必须有等待、人工阻塞或证据终态；市场波动必须按日频/月频子数据集分别判断新鲜度。→ 方案 §12.7

### 复发链证据（第三轮最终表述）

20260915 二次根治方案 §6.1(2) 关于“运行时与历史调和调用同一实现”的告警生命周期部分已经落地：`verifyAlertScope` 位于 `server/services/jobAlertMailer.js:79`，运行时告警关闭调用它，历史脚本只是兼容包装。

真正未落地的是**中立槽位恢复证据层**：`jobScheduleSlots.js:232-262` 两条水位恢复路径仍只查水位就把槽位改为 `succeeded`；而现有 `verifyAlertScope` 又要求槽位已经成功或存在后续成功，不能直接用于状态修改前的裁决。INC-0026 的「假成功」说明必须把运行、阶段、分区和水位判断下沉为唯一共享核心，而不是只增加一次函数调用。

为此保留“逐条落地追踪表”（方案 §12.3）：条目 → 代码位置 → 测试用例 → 生产证据。追踪表先登记进 `docs/知识索引.md`；只有实际修改 `scripts/check-knowledge.js` 并新增测试后，才能宣称它已纳入自动门禁。

状态更新为**代码整改已完成，本地回归通过**：代码和研发口径已对齐；生产恢复、运行编号和连续 3 个交易日观察尚未执行，不得标记为生产已修复。

## 补录（2026-09-16）：线上告警现状核查与状态防御补强

生产版本 `0.8.1.42` 的 Web、Worker、健康检查定时器均正常。只读核查发现 18 条仍开放告警，今天的可转债行情数据集、公司财务同步和下游安全/估值阻断仍有真实证据；CNINFO `topSearch` 仍为 403 权限拒绝，不能自动关闭。另有多个历史槽位的 `job_schedule_slots.status=succeeded` 与最新 `job_runs.result_json.ok=false` 不一致，不能把槽位状态当作恢复证据。

本地新增最小防御：`completeSlot()` 在写入成功状态前复核结果状态，失败结果强制落为 `failed`，并新增数据库回归断言。该改动尚未生产发布；生产定向补跑、数据补写和告警收敛仍需单独授权。

## 补录（2026-09-17）：线上告警手动补跑复核

按用户授权，先执行生产备份，再对当前告警链路执行一次有界人工补跑：

- `810647` 可转债行情同步 → 运行 `3005`，仍为 `partial`；目标日 `2026-09-16` 有 `255` 条有效价格、`140` 条 `retryable_missing`，因此 `bond_daily` 未完整发布。
- `810649` 可转债公告同步 → 运行 `3004`，调用上交所 `22` 次、深交所 `59` 次，仍因分页不完整失败，未伪造公告分区成功。
- `810648` 下修动机输入 → 运行 `3003`，10 次 Tushare 调用全部完成且 `failures=[]`，但仍有 `498` 个对象待续跑（持有人 `249`、质押 `249`）。根因不只是缺少槽位上限：该服务每批重新加载全部候选，固定 10 次只能轮换前少量对象，结果又始终因“配置了 limit”返回 `continuationRequired`，因此重复运行无法达到完成态。现已取消该任务固定调用数，默认完整遍历实际候选。
- `769365` 港股 IPO 补全 → 运行 `3002`，17 次外部调用后仍为 `degraded`，`hk_ipo_facts` 未完整发布，但已形成 `2026-09-17` 水位。
- `810652` 估值和 `810637` 安全评分各手动入队一次；调度器依据 `810647=failed` 的依赖证据重新阻塞，未执行无效派生计算。

最终线上版本为 `0.8.1.54`，Web、Worker、健康检查定时器、`/health`、`/ready` 均正常。开放告警扫描为 `48` 条（`pending=3、sent=45`），恢复候选 `0`、关闭 `0`。CNINFO 权限拒绝、Tushare 权限/额度、交易所分页缺口和数据分区缺口均没有恢复证据，因此没有手工关闭或批量清零；外部熔断的 `probe_in_flight` 均为 `false`。

补充核对：生产 `top10_cb_holders` 主凭据熔断当前为 `closed`，本次运行没有接口失败。进一步全量核查发现迁移 128/130 曾批量写入 450/180/90000，并为 `top10_cb_holders` 写入 30 次/分钟；这些值随后被测试固化，导致先前只检查通配策略的门禁仍能通过。Tushare 官方当前只给出账号积分档位和各接口积分/单次行数要求，`top10_cb_holders` 页面未公布 30 次/分钟。迁移 159 因此清空全部 `internal_*`，数据库和应用入口同时禁止回写；权限拒绝改存接口权限阻塞，所有仍为 `open` 的临时熔断强制带 `recover_at`。

## 补录（2026-09-16）：公司财务空跑核验与恢复日期修复

原公司财务运行 `2913` 于 11:47 在修复版本发布前失败，直接原因是当天没有 `stock_financial_reports` 已发布分区；表内仍保留 11657 条真实财务事实，最近披露日为 2026-08-31。按用户授权重跑槽位 `769340` 后，运行 `2925` 于 15:01 成功，Tushare 调用 2 次，并发布 2026-09-16 核验分区：`queue_count=0`、`changed_rows=0`、`coverage_status=verified_no_change`，没有伪造新增财报。

成功后旧告警未自动收敛，进一步定位为 PostgreSQL `Date` 在 Node 侧以带时区的 JavaScript `Date` 返回，恢复证据用 UTC 截断后把业务日错判为前一天。`server/services/jobRecoveryEvidence.js` 新增按 `Asia/Shanghai` 规范化业务日的 `businessDateText()`，本地全量回归 132 通过、0 失败、0 跳过；生产已依据成功运行和已发布分区证据关闭告警 `19914`、`19923`、`19929`。该日期转换代码尚未单独生产发布，后续标准发布后由运行时自动收敛复用。

## 补录（2026-09-17）：告警作用域与港股 IPO 假失败继续修复

本轮生产只读核查又确认两处会让后台告警长期不收敛的代码根因：

- 数据集熔断/缺口告警原先用槽位业务日组成 `dataset:scope:partition`。对于 `convertible_bond_universe_refresh`，槽位业务日为 `2026-09-17`，实际目标分区为前一交易日 `2026-09-16`，所以告警 `19956` 的作用域无法命中真实的 `bond_daily|CN|2026-09-16` 分区证据。
- 港股 IPO 配发、非公开上市、取消上市和招股书同步的成功收尾分支，在没有失败消息且没有显式批次上限时把 `NULL` 传给非空的 `ops.ingestion_runs.error_message`。生产运行 `3015` 已记录该约束错误，随后被错误累计为 `hk_ipo_facts` 数据集失败。

本地已做最小修复：`jobOrchestrator.js` 优先从结果目标日/分区日/缺口日生成告警分区键，缺失时才回退 `expectedDataDate()`；`jobRecoveryEvidence.js` 严格按数据集注册表的 `scopeKey` 匹配恢复分区；`hkexIpo.js` 四个成功收尾分支改写空字符串。专项断言和全量测试均通过（`134/0/0`）。

当前状态：`0.8.1.57` 已完成生产发布并按授权完成 `810647`、`769365` 有界补跑；后续 `0.8.1.58` 已修复港股 IPO 模式恢复误判并完成标准发布。生产告警只按真实分区/对象证据处理，未批量清理。

## 补充（2026-09-17，0.8.1.57 发布与 0.8.1.58 追修）

版本 `0.8.1.57` 已完成标准生产发布。按授权定向补跑后，`769365` 运行 `3059` 已成功收尾，`hk_ipo_facts` 已发布且 `failedDatasets=[]`；`810647` 后续运行 `3065` 仍有 `140` 条可转债价格缺口，保留真实失败状态。

补跑后的恢复核对又发现：`769365` 历史槽位的 `request_payload.mode` 仍为 `core`，而任务实际结果为 `enrichment`，恢复证据因此返回 `run_mode_mismatch`，旧的槽位告警不能自动收敛。版本 `0.8.1.58` 已补充最小修复：任务定义声明固有模式，默认槽位继承定义模式，恢复证据优先按定义模式判断。版本已完成标准生产发布，提交 `f977ec6`，Web/Worker/健康检查定时器、`/health`、`/ready` 均通过；既有恢复入口已按运行 `3059` 的成功证据关闭 `19935`。可转债 `19956` 已按运行 `3065` 的真实证据改为正确作用域 `bond_daily:CN:2026-09-16`，但 `255/395`、缺 `140` 条仍未完成，未关闭；`19940` 继续待处理。未重复采集已成功的港股 IPO 数据，也未改写真实缺口状态。

## 补录（2026-09-19）：可转债行情 140 条假缺口根因

生产 2026-09-16 至 18 日连续出现 `bond_daily` 部分发布，三日分别为 `255/395`、`254/395`、`254/394`。持续缺失告警只显示连续三日的 8 只，但持久化队列实际仍有 140 只。

只读核查确认，旧 `activeProfile()` 把 Tushare `cb_basic` 的 `YYYYMMDD` 生命周期日期与调度器传入的 `YYYY-MM-DD` 目标日直接做字符串比较，历史退市、到期和停止转股日期因此没有生效；同时旧逻辑允许上市日为空，也没有复用发行事实中的定向/私募过滤。140 条由 130 只历史退市债、7 只定向债和 3 只未上市债组成，不是真实行情源缺口。

按最小调用原则仅对 `cb_basic` 和 `cb_daily(20260918)` 各查询一次：生命周期候选 313 只，排除定向/私募后公开在市 310 只，310/310 均有有效价格；`cb_daily` 额外 4 条为 2 只已停止转股债券和 2 只可交换债，均不应纳入预期集合。代码现复用 `bondSafetyTushare.isActiveBond()` 与 `filterPublicBonds()`，并增加带横线目标日、空上市日和定向债过滤回归。提交 `5277bf4` 已完成标准生产发布，`/health=0.8.1.69`、`/ready=ready`，Web/Worker/健康检查定时器均通过；生产任务补跑和告警恢复仍待单独授权。

## 补录（2026-09-19）：生产补跑与依赖阻塞告警恢复

用户授权后串行补跑 `892480`、`810647`、`851336`，运行 `3124`、`3127`、`3128` 均成功；2026-09-16、17、18 三日 `bond_daily` 分区分别重发为 `311/311`、`310/310`、`310/310`，缺口为 0，历史数据集告警已按分区证据自动收敛。下游估值 `892484`（运行 `3125`）与安全评分 `892477`（运行 `3126`）也已成功，两个派生分区均发布。

恢复预览继续发现 `dependency_blocked` 被 `verifyAlertScope()` 以数据绑定告警为由提前拒绝，没有进入已有的 `verifySlotRecoveryEvidence()`；后者本身已明确支持依赖阻塞并校验运行、阶段、严格分区和水位。版本 `0.8.1.70` 最小修正该分流，只允许依赖阻塞进入严格证据层，数据质量告警仍要求数据集作用域。提交 `c46a79b` 已完成标准生产发布，线上健康、就绪和服务状态均通过；发布后预览只确认 `21206`、`21189` 具备 `slot_success_and_dataset_evidence`，已按明确 ID 关闭。此前三日数据质量告警和安全评分超时告警也均已由对应成功分区/槽位自动关闭，恢复通知保留为 `sent` 历史记录。
