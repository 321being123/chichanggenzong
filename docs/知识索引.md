# 存在小站知识索引

## 2026-09-10 强达转债申请户数漏补（0.8.1.10 本地已修复）

- 强达转债（123284）发行结果公告披露配号总数 `10,525,519,348` 个，按深市既有口径估算申请户数为 `1052.5519348` 万户，本地发行事实已补齐；页面统一显示为两位小数 `1052.55（估算）`，数据库保留原始精度。
- 根因是公告 PDF 将“配号总数”拆行，旧解析未先清理版面空白；同时自动补全错误依赖上市日期，导致结果公告已到期但上市日期暂缺时不进入队列。现已改为按结果公告日到期筛选，并兼容 PDF 拆行。
- 生产只读核查确认近期页面涉及的 123282、123283 已有估算值，123284 是本次新增缺口；123255、123266 为既有公告字段不可用记录，不伪造数值。生产补数与发布待用户单独授权。

## 2026-09-10 招商证券净值快照缺口与港股代码残留（0.8.1.9 已修复）

- 招商账户一笔“深圳国际”历史交易仍为错误代码 `000152`/深市/CNY，导致 `nav_snapshot` 找不到行情后静默跳过 9 月 8、9 日；华泰因每日已有快照未暴露该问题。
- 已改为优先使用券商导入或人工校准持仓快照（招商 8 月 24 日 `00152`、12,737 股）作为锚点，只重放锚点后交易；缺行情/汇率返回部分失败并进入统一告警；页面缺上一交易日快照不再显示“今日涨跌”。
- 生产代码修复提交 `83dd249`，最终发布树提交 `4640372`；错误快照清零，8 月 24 日人工锚点保留 12,737 股，9/8、9/9 净值已补齐。更早日期仍有缺行情/汇率缺口，任务会明确返回 `partial`，不得伪装成功。生产修正、补快照和验收凭证见 [INC-0015](incidents/INC-0015-招商证券净值快照缺口.md)。

> 范围：仅 `portfolio-server`。这是所有 Agent 的项目知识入口，不是历史资料全集。
> 更新：2026-09-10。

## 2026-09-10 港股盘中认购倍数华盛备源（0.8.1.6 本地已实现）

- 利弗莫尔盘中接口返回空、升级提示或请求失败时，自动调用华盛公开新股接口，解析预计孖展倍数并按本地申购窗口校验后写入 `analytics.hk_ipo_market_snapshots`，来源标记为 `vbkr-public`，原始响应写入 `ops.raw_records`。
- 前端明确显示“华盛（捷利数据）预计孖展”，不冒充港交所官方实时或最终超购；两来源均不可用时保留上一份有效快照。新增迁移 150、任务接口矩阵及回归测试；本地全量测试 127 项通过、知识治理检查通过，未执行生产回补或部署。

## 2026-09-09 港股打新中文名、申购倍数与暗盘信号（0.8.1.0 已部署）

- 新增港股中文展示字段 `ipo_history.security_name_cn`；港股日历和历史页优先显示中文名，00100/03887/06880 使用已审核的中文品牌别名，其余缺失时显示“中文名待补”，不把英文注册名冒充中文名。
- 新增 `analytics.hk_ipo_market_snapshots`，将申购期认购倍数与最终配售倍数分开；申购期数据只接受招股窗口内的动态值，按来源和更新时间展示，失败保留上一份有效值。
- 港股历史页新增绿鞋结构判断、利弗莫尔暗盘涨幅、富途暗盘涨幅。绿鞋判断仅表示稳价安排的结构性影响，不等于正式投资建议；港股正式建议门禁仍为失败闭合。
- `hk_ipo_preopen`、`hk_ipo_enrichment` 已纳入利弗莫尔/富途来源级 Guard、原始响应审计和每日调用上限。版本 `0.8.1.0`（提交 `1b25022`）已推送并完成生产部署，迁移 148/149 已执行，Web、Worker、健康检查定时器和 `/health` 均验收通过。生产历史信号补数未执行，后续由日常任务增量写入或另行授权补数。详细规则见[港股打新日历与严选建议研发方案](港股打新日历与严选建议研发方案_20260907.md)和[技术架构](技术架构.md)。

## 2026-09-09 换股“终止上市”公告误关套利案件（已修复并完成生产验收）

- 08:30 巨潮增量同步抓到东兴证券、信达证券“连续停牌直至终止上市、实施换股吸收合并”公告；旧规则把“终止上市”误当成交易终止，案件 208/209 被置为 `terminated`，公开列表和实时换股收益随之消失。
- `detectUpdate`、公告角色识别和公开列表终态兜底均已排除“终止上市”，仍保留明确“终止本次交易/要约”等真实终态；案件重建新增“完整换股三元组优先”保护，并兼容 `SSE/SZSE` 到 A 股 `.SH/.SZ` 参考代码，版本记录为 `0.8.0.37`。
- 已按授权完成生产修复：备份 `/var/backups/portfolio_arbitrage_before_restore_20260909_165445.dump` 后恢复 208/209 为 `proposed`，回填官方解析证据中的固定换股价和 `601995.SH` 参考证券；公网列表/详情实时换股收益分别恢复为东兴 `7.94%`、信达 `6.10%`。详细记录见 [INC-0014](incidents/INC-0014-换股终止上市公告误关套利案件.md)。

## 2026-09-09 可转债强赎公告状态回退修复（本地已实现，生产待授权）

- 统一口径：提前赎回公告、到期兑付/停止交易公告都算“已公告强赎”，即 `business_status=announced`；数学进度 `calculated_status=tracking` 只用于展示触发进度，不能覆盖公告状态。
- 根因：强赎统一视图把最新 `warning`（触发提示）也当成了最新事件，可能覆盖已经入库的 `implementation` 公告，导致状态回退为“跟踪中”。迁移 147 改为只从 `exercise`、`implementation`、`waive`、`completion` 取最新有效决策事件；到期公告在同步层已归入 `implementation`，与提前赎回统一处理。
- 生产只读核查同类临期债共 5 只：南航转债（110075）、天能转债（123071）、乐歌转债（123072）、鸿路转债（128134）、洽洽转债（128135）。本地定向回归与 `npm.cmd run test:all` 均通过（126 项、0 失败、0 跳过）；未执行生产迁移、补数或部署。详细记录见 [INC-0013](incidents/INC-0013-可转债强赎公告状态回退.md)。

## 2026-09-09 INC-0010 宇邦转债停牌缺口修复（本地已实施，生产待授权）

- 主任务 `convertible_bond_universe_refresh` 已启用严格数据集发布门禁，主任务产出的 6 个数据集全部进入分区白名单；白名单交集为空或不完整时失败关闭，不再因主行情水位新鲜而误判整条任务完成。
- `suspend_d` 成功返回空名单登记为 `verified_no_suspension`，接口失败登记为 `unknown/stale` 并保留上一份有效数据；历史缺失日合并成连续区间补取，正股行情补漏扣除已核实停牌日。
- 停牌数据集失败重试只传递 `failedDatasets` 进入停牌阶段，连续 3 次未完成后任务进入 `blocked` 并告警；强赎接口返回逐债 `diagnostics`，页面显示停牌缺失日期，`waived` 与 `incomplete` 分开统计。数据库迁移 146 约束 `data_status` 为 `complete/incomplete/pending`，业务状态继续独立表达。
- 相关代码、迁移、测试、页面和版本记录已更新；本地迁移版本已到 146，`npm.cmd run test:all` 通过 126 项、失败 0 项，`npm.cmd run check:knowledge` 通过；本地 `/health` 返回 `0.8.0.33`，相关 API 和脚本静态资源验收通过。未执行生产历史补数、生产迁移或部署。完整方案与实施记录见 [INC-0010](incidents/INC-0010-宇邦转债停牌日缺口与任务误判完成.md)。

## 2026-09-09 打新日历可转债流通规模漏显示（已修复并纳入现有定时任务）

### 流通规模口径修正

- 首次回填的 `0.4234` 亿元是错误旧事实：程序把原股东合计配售 `5,651,932` 张全部当成限售。
- 正确应读取发行结果公告中的控股股东、实际控制人及一致行动人配售 `4,551,066` 张；发行总量 `6,075,298` 张，流通规模为 `1,524,232` 张，即 `1.5242` 亿元。
- 代码已改为多份结果公告优先选择“中签率/优先配售结果”公告，并禁止用原股东合计配售量冒充限售量；指定代码可定向幂等覆盖旧记录，普通增量仍跳过已入库事实。

- 根因不是公告没有流通规模，而是日报已切换为数据库只读后，负责写入 `analytics.convertible_bond_listing_liquidity` 的生产同步没有并入现有生命周期任务；同时旧解析器找到上市公告书后，遇到没有可识别“前十名可转换公司债券持有人”表格的 PDF 就直接失败，不会继续尝试发行结果公告或公告中的上市数量。
- 已修复：复用现有 `convertible_bond_announcement_history_sync` 的 17:30 槽位，近 60 天未入库候选单次最多 5 只；解析顺序为持有人明细精确值 → 发行结果公告控股股东/实际控制人/一致行动人配售量 → 上市公告书明确上市数量兜底，`source_detail.quality` 记录口径，不覆盖失败结果。
- 已验证 123283：版本 `0.8.0.32`（提交 `7993052`）已部署；生产备份后定向回填保存 1 条，数据库流通规模已修正为 `1.5242` 亿元、限售规模 `4.5511` 亿元，质量标记为 `issue_result_controller_allotment`。近 60 天已入库的 24 条同类上市事实中，没有遗留 `estimated_original_shareholder_allotment` 质量记录；123285 尚未上市，暂不生成上市流通规模。备份：`/var/backups/portfolio_cb_listing_liquidity_20260909_022248.dump`。

## 2026-09-08 打新日历漏同步可转债申购与上市（已部署并完成生产补录）

- 丰茂转债（123283）2026-09-09上市、润禾转02（123285）2026-09-08申购未进入日历，根因是08:00任务只同步到上一交易日、上市公告未写入生命周期事件，以及18:05日历缺少可转债发行事件分区依赖。
- 已把解决方案并入现有定时任务：保留07:40统一公告任务并增加17:30当天补充槽位；同批公告在本地分类为生命周期、下修和强赎；原07:45强赎任务退出；`cb_issue`从08:00主档任务移到17:30且每天只调用一次。
- 可转债生命周期由60天固定回扫改为“首次全量＋成功游标回退3天”；新增官方PDF解析、`bond_issuance_events`质量门禁，18:05打新日历同时依赖新股事实和可转债发行事件。`0.8.0.30`（`4b5ce86`）已部署生产；补录覆盖 9 月 5 日至 9 月 8 日，最新 9 月 9 日日报已核验包含 123283 上市和 123285 申购。完整记录见 [INC-0012](incidents/INC-0012-打新日历漏同步可转债申购与上市.md)。

## 2026-09-08 中金现金选择权价格串入 H 股与公告漏读（三笔重解析完成，巨潮公告同步待自动重试）

- 线上只读核查确认：案件 `case_id=210` 的目标是 `601995.SH`，页面 `32.58` 是 A 股实时价；`03908.HK` 实时价为 `19.90`。异常字段 `18.86` 来自报告书中“A股34.80元/股、H股18.86港元/股”的并列条款，解析器把 H 股价写入了 A 股现金退出价，导致页面显示 `cash_choice_price=18.86`、预期收益 `-42.11%`。
- 2026-09-08 发布的官方实施公告明确 A 股异议股东收购请求权行权价格为 `34.57元/股`，并披露 9 月 15 日起停牌、9 月 15–17 日申报。生产 9 月 7 日晚同步运行范围只到 9 月 7 日，任务本身成功但源站尚按 9 月 8 日发布日索引，因此 `raw_records` 和 `event.documents` 没有这些新公告；早期毫秒时间戳还存在 UTC 前移一天的日期解析缺陷。
- 已发布修复：A/H 现金条款按市场/币种隔离，支持“行权价格”；现金选择权实施公告进入条款更新角色；巨潮时间戳和同步截止日统一按上海日期；套利同步调整到每日 08:30。另修正重解析任务的官方 PDF 外部请求预算、限速等待编排、失败缓存判断、解析状态读取、Python 限速错误透传、PostgreSQL 日期游标归一化和公告同步恢复时间传递。`0.8.0.27` 已加入公告 PDF 30天/5 GiB 最旧优先缓存，并取消巨潮本站来源级500次/日保护，仅保留20次/分钟；日调用量继续入库，真实上游429/额度错误记录熔断时间、恢复时间和错误详情。
- 详细根因、同类范围、上线后的补数步骤和遗留风险见 [INC-0011](incidents/INC-0011-中金现金选择权串入H股价格与公告漏读.md)。
- 生产重跑时进一步确认两层根因：任务传给 Python 的目标代码为 `601995.SH`，旧解析器只识别纯数字代码；去除后缀后又发现合并报告中的 `a_cash_matches[-1]` 会把中金最后一组价格跨公司写给东兴、信达。`0.8.0.28` 已修复市场后缀，`0.8.0.29` 已改为按目标公司简称和最近公司上下文选择条款，找不到目标条款时不再使用其他公司的兜底价格；解析器版本为 `2.0.6`。本地全量测试为 123 通过、0 失败、0 跳过；生产重解析槽位 `548122/548123/548124` 已成功，东兴/信达/中金分别为 `13.04/17.75/34.57`，均 `validated`。
- 公告同步槽位 `544851` 已手动唤醒执行：港交所游标成功推进至上海日期 `2026-09-08`；巨潮 `2026-09-07~2026-09-08` 窗口仍因网络请求超时失败，并非额度耗尽或熔断，自动重试时间为北京时间 `2026-09-08 21:11:33`。巨潮日调用计数为 `625`、`budget_limit=NULL`，当前没有开放的巨潮熔断；缓存 128 个 PDF、约 284 MB，远低于 5 GiB 上限。

## 2026-09-08 港股绿鞋比例修复生产部署（0.8.0.15 已上线）

- 提交 `8b8f771` 已按标准流程推送并部署；生产迁移 140–144 执行成功，`portfolio-server.service`、`portfolio-worker.service`、`portfolio-worker-health.timer` 均为 `active/enabled`，线上 `/health` 返回 `0.8.0.16`，首页静态资源为 `js/ipo.js?v=0.8.0.16`。
- 绿鞋保护比例统一使用回拨后的最终香港公开发售股数，保留初始/最终股数和公告证据；代码部署后已按用户授权完成生产历史数据同步，后续同步继续沿用最终公开发售股数口径。

## 2026-09-08 港股历史数据生产同步（已完成）

- 已完成生产备份后执行港交所官方历史回填：新上市报表批次 `1914` 写入 172 条；招股书批次 `1917` 成功补全 166 条，6 条因未找到官方招股书保留降级记录；配发结果批次 `1918` 成功补全 162 条，4 条因公告缺少完整初始发售结构/一手中签率保留降级记录。
- 港股日线覆盖批次 `1919` 写入 4,188 条，175 个候选首日覆盖 100%，五个交易日覆盖 99.43%；生产港股历史接口现有 175 条，159 条绿鞋记录使用 `final_public_offer_after_reallocation`。
- 生产抽查 `09615.HK`：初始公开发售 1,157,040 股，最终公开发售 4,628,130 股，超配 3,471,060 股，绿鞋保护比例 74.9992%。旧初始股数口径残留比例为 0 条。
- 已按现有数据运行生产历史回测并落库（`backtest_id=1`），正式建议门禁仍为 `blocked`：五日回测覆盖 75.90%、双环境探针稳定日不足、walk-forward 窗口不足及正式收益指标未达标；因此继续只展示事实和研究结果，不开放正式建议。

## 2026-09-08 港股打新门禁最终复核（覆盖阻塞已清除，正式建议继续阻断）

- 2026-09-08 00:09（Asia/Shanghai）通过服务器只读 `curl` 访问港交所官方主板/GEM入口，本地复用同一解析器得到主板 4 行、GEM `empty_unconfirmed`，HTTP 均为 200；结果以 `environment=server` 写入 `ops.ingestion_runs/raw_records`，未上传代码、未改生产数据。
- 双环境同日交集为 2 个（`2026-09-07`、`2026-09-08`），固定夹具通过；仍未达到连续 5 个 HKEX 开市日。官方配发 PDF 的首档一手中签率覆盖推荐档 162/162（100%），全部已上市候选 165/165；官方英文招股书定向重解析后，保荐人分组覆盖已补齐为 165/165（100%），集中度分组覆盖阻塞已清除。最新回测为 `backtest_id=73`，模型配置仍为 `research_only`，walk-forward 为 2 个窗口，正式建议门禁继续 `blocked`。本轮兼容跨页参与方标题、`Sponsor-OCs` 多角色表头、单独 `Sponsor`、单数 `Intermediary` 和 `Hong Kong SAR` 地址边界，全部保留官方证据，未使用估算值。
- 本轮修复配发 PDF 的部分解析丢失：当公开/国际发售比例不完整但一手中签率已被官方 PDF 解析时，现保留已审计的一手中签率，比例字段保持空值；已用 02476 官方 PDF 实际回写验证（0.71%），未把不完整结构误标为完整。
- 默认增量任务已补上重复抓取保护：对已确认 `parserStatus=incomplete` 且 `lotteryParserStatus=parsed` 的官方文件默认跳过，仅显式 `refreshLottery=true` 才重试；相关回归、全量测试均通过。
- 配发补全候选已排除官方确认的介绍上市、GEM 转主板和 De-SPAC 项目，避免对非公众项目重复检索；相关回归与全量测试通过。
- 历史回测（`backtest_id=70`、`71`）已被本次最终复核的 `backtest_id=73` 替代；当前候选 165、可回测 165、推荐档 162、测试样本 78、首日/五日覆盖均 100%、硬否决 0。已完成生产代码部署，未执行生产历史数据同步。
- 本轮验收发现旧版招股书解析器曾把正文/角色说明误写为保荐人值，已清理本地错误派生字段；保荐人解析器已升级为 v2，并补充正文误识别、表头同/换行、协调人单复数、跨页标题和单独 Sponsor 角色回归测试。当前 165/165 条标准字段、文档证据和原始载荷均为规范化公司名单，无短词、角色标题或地址等脏值。
- `backtest_id=73` 的剩余阻塞为真实绩效指标、双环境稳定日 2/5、walk-forward 2/3；`hk_daily` 首日/第五交易日覆盖 165/165，分组 108 组且最大组占比 10.49%。本轮另清除了 2 条旧版 `and` 派生脏值，当前全量角色/地址/短词反查为 0。正式建议快照入口继续拒绝未通过门禁的写入，已完成生产代码部署，未执行生产历史数据同步。
- 只读评分敏感性检查显示：推荐门槛 78～90 均选中 162 条，门槛 95/100 仅剩 139 条，但正式收益指标仍不达标；当前不能用门槛调整代替新模型评审。
- 模型输入复核显示：165 条上市样本的 `issue_pe`、`industry_pe`、募资额、总股本和公开认购倍数均无值；当前模型只能使用少量发行结构字段，补齐官方历史特征并生成新模型版本是解决收益阻塞的必要路径。
- 港股配发公告事实链新增超额认购倍数和绿鞋保护解析：倍数写入 `ipo_history.oversubscribe_multiple`，绿鞋状态、超配股数、初始公开发售股数、回拨后最终公开发售股数及 `protectionRatioPct=overAllocatedShares/finalPublicOfferShares×100%` 写入 `greenshoe_details`，保留 `publicOfferSharesBasis=final_public_offer_after_reallocation`、`factsParserVersion=hk-ipo-allotment-facts-v5` 和原文证据；接口增加 `greenshoe_protection_ratio`、初始/最终公开发售股数字段，页面“绿鞋保护”列展示“绿鞋/公开发售”比例。公告未披露最终数目时不沿用初始数目估算；无稳定价格机制与完全未披露分别显示。

## 2026-09-07 宇邦转债强赎数据不完整（根因已核实，方案待实施）

- 生产宇邦正股 2026-09-01 停牌记录缺失，强赎和下修均受影响；证券分析强赎字段实际也为不完整。根因是历史主链预算中断后，重试仅依据主行情游标误判 `fresh`，且停牌同步未补历史失败日。
- 线上强赎列表 310 只中，290 只数据完整；1 只宇邦存在日期缺口，另 19 只为承诺期内零观察日被混标为不完整。详细证据、复现和沿用现有架构的方案见 [INC-0010](incidents/INC-0010-宇邦转债停牌日缺口与任务误判完成.md)。本次仅诊断和方案，未修改业务代码、补数或部署。

## 2026-09-07 港股打新日历与严选建议（P0 链路完成，正式建议门禁保持阻断）

- [港股打新日历与严选建议研发方案](港股打新日历与严选建议研发方案_20260907.md) 已追加第三轮评审修订；修订条款优先于原文。新增统一证券身份与 IPO 事实表边界、上市前后复用同一 `instrument_id` 的生命周期、当前数据库核对结果，以及港交所官方事实/项目网络实测/内部 Guard/计划上限四级限制口径。`60 次/分钟、2000 次/日` 明确为内部保护线，`www2` 加白和 `6/12/20` 为计划动作，均不得称为港交所官方限制。未通过修订后的回测门禁前，只显示事实及“研究评分，待校准”。
- Tushare 主账号实测 `hk_basic`、`hk_tradecal`、`hk_daily` 可用，其中 `hk_daily` 是当前账号额外开通的独立权限，并非 6000 积分自动获得；`hk_income`、`hk_fina_indicator` 当前主备账号均无权限。港交所官方披露作为招股、定价、发售结构、基石、回拨和配售结果的核心事实源。
- 本轮已完成 P0 链路接入并留存真实验收证据：本机与服务器官方主板/GEM 入口均 HTTP 200，两个环境探针摘要均落入 `ops.ingestion_runs/raw_records`，服务器记录含响应哈希和远端解析状态；HKEX 日历本地入库 518 行；港交所官方主板/GEM 历史 Excel 报表已接入，2025-08-04 至 2026-09-07 本地回填 171 条港股 IPO 事实。`hk_daily` 已改为未覆盖证券筛选、官方单证券请求、每轮限频、失败保留、游标和首日/五日统计；因当前账号“频率超限（1 次/小时）”，新增腾讯港股 K 线历史补漏入口，已有 4,182 条港股日线，171/171 候选完成首日/五日覆盖，且严格按 HKEX 开市日匹配首日和第五交易日；Tushare 与腾讯重叠样本逐日价格一致。新增港交所 `15100` 配发结果标题检索、英文 PDF 初始发售结构解析，以及官方招股书 `30700` 检索与 PDF 解析；官方文件进一步确认 6 条非公众项目（2 条介绍上市、3 条 GEM 转主板、1 条 De-SPAC），已写入 `ipo_status`、统一主档状态和 `ops.raw_records`，不再进入公众招股回测队列。其余 165/165 条公众招股候选的发行价区间、每手股数、公开发售开始和截止时间均有官方招股书证据，原始响应、哈希和解析状态均留存于 `ops.raw_records`。正式验证器 `hk-ipo-formal-audit-v1` 已实现成本、后验区间、净收益和集中度检查，但模型配置仍为 `research_only`；截至 2026-09-07 初始回测为 165 条公众招股候选、161 条推荐档、测试样本 78、首日/五日覆盖 100%、自然月 walk-forward 窗口 2 个，硬否决计数为 0；次日复核已更新到本节顶部，正式快照写入口持续强制先过门禁，港股历史接口返回 `formal_gate`，未执行生产数据同步或部署。

## 2026-09-07 移动端可转债名称列错位修复（本地已实现）

- 共享 `BusinessTable` 浮动表头不再重复叠加横向滚动补偿，手机端转债名称表头按源表头实际位置对齐；上市转债、强赎、下修、安全性和持仓等复用身份列的表格统一生效。
- 已补充移动端 DOM 回归断言，确认横向滚动后浮动身份列表头不再使用错误的滚动量偏移。

## 2026-09-06 持仓休市涨跌与零持仓导出修复（本地已实现）

- 休市日不再请求或显示最近交易日的涨跌；前端今日总资产涨跌、单只持仓今日盈亏和后端净值归因均以有效交易日为边界。
- 持仓 Excel 导出与页面列表统一过滤 `quantity > 0`，历史数量为 0 的持仓残留不再导出；已补充前端、路由和零持仓回归断言。

## 2026-09-06 可转债公告事实同步取消交易所内部限额（0.8.0.7 已生产发布）

- 根因是“可转债下修与转股价公告事实同步”任务原有单批 20 次外部请求上限，同时上交所、深交所来源策略还设置了 60 次/分钟、2000 次/日的内部预算；官方 PDF 解析会叠加请求，正常同步因此频繁触发预算告警。
- 任务单批上限已调整为 600 次，覆盖交易所批量公告和官方 PDF 解析；迁移137将 `sse`、`szse` 的内部分钟/日预算置空，Node/Python 默认策略同步取消这两项限制，保留并发去重与真实上游异常处理。巨潮 `20次/分钟、500次/日` 保护线不变。
- 已补充任务预算和交易所默认策略回归检查；版本 `0.8.0.7` 提交 `9a200ed` 已按标准流程生产发布，迁移137执行成功，Web/Worker/健康检查定时器通过，`/health` 返回 `0.8.0.7`。本次未执行历史数据同步。

## 2026-09-06 网站数据看板（0.8.0.9 已生产发布，统计密钥待配置）

- [网站数据看板设计方案](网站数据看板设计方案_20260906.md) 已按执行方案落地：迁移138新增 `ops.site_events`、`ops.site_runtime_minute` 和 `ops.sync_cursors.cursor_payload`；新增 `site_analytics_retention` 每日02:00清理，默认保留30天。
- 新增公开 `POST /api/telemetry/events` 与后台 `/api/admin/analytics/{overview,traffic,behavior,runtime,data-health}`。公开入口沿用 CSRF、32KB请求体限制、IP/访客双限流和白名单事件；看板要求 `ops_manage`，内部流量默认排除，成功事件由后端确认。
- 共享埋点脚本覆盖主站、登录、打新报告、知识分享和下修详情等入口；请求日志/事件循环复用现有入口做分钟采样，Nginx 采集复用同步游标并从日志尾部初始化，统计接口 429/5xx 单独记录。看板补充真实新增注册、金融数据分区/质量/任务/预算/熔断、数据库/Worker 状态、来源域名、30分钟会话、退出统计和有限重试；迁移139为既有运行表补齐独立429字段；Redis 使用实际 PING，`/ready` 与看板共用健康状态。
- 已通过本地语法检查、看板纯函数/Nginx解析测试和项目完整自动测试；生产迁移 138/139、Nginx 检查与 reload、标准部署均已完成。提交 `8a18276`、版本 `0.8.0.9` 已生产发布，Web/Worker/健康检查定时器均为 `active/enabled`，`/health` 返回 `0.8.0.9`，后台未登录访问按预期返回 302，看板脚本返回 200；本次未执行生产数据同步。
- 生产 `.env` 当前未配置 `SITE_ANALYTICS_SECRET`，因此访客/用户 HMAC 标识、UV 和会话串联不能作为有效统计；配置真实随机密钥和来源后，仍需补做事件链路、`/ready`、浏览器视觉与真机验收。实现细节和剩余门槛以方案执行记录、`docs/技术架构.md` 和本次代码为准。

## 2026-09-06 CI full-test 无主档映射回退修复

- GitHub CI #416 的 `full-test` 失败集中在 `ipo-report/test_ipo_unit.py`：测试库没有证券主档映射时，`301677` 和 `600000` 未补出 Tushare 的 `.SZ/.SH` 后缀；Node 测试及其他 CI 检查均通过。
- 根因是 Python 统一身份模块的无品种回退只识别可转债，未覆盖打新入口同时处理的 A 股代码。已将默认市场推导收敛到 `ipo-report/instrument_identity.py`，并补充无映射回归断言；不改变已有主档映射优先级。
- 本地 `CI=1 npm.cmd run test:all` 已通过 **117 通过、0 失败、0 跳过**，完整证据以本次代码与测试结果为准。

## 2026-09-06 网站移动端适配与开发标准（已部署，真机验收待执行）

- [网站移动端适配与交互设计方案](网站移动端适配与交互设计方案_20260905.md) 已按原方案实施手机导航、共享表格、图表点按、输入弹窗、阅读页和后台适配，保持原业务字段、数据及权限链路。
- 后续前端改动必须兼容移动端，强制入口在 `AGENTS.md`，实际规范与共享能力见《技术架构》第 5.3 节；新增 DOM 行为回归已通过 61 项，纳入全量测试。
- 当前版本 `0.8.0.6` 已提交 `14439dc` 并按标准流程生产部署；全量测试 117 通过、0 失败、0 跳过，知识门禁与版本/资源静态检查通过。生产 Web、Worker、健康检查定时器均为 `active/enabled`，`/health` 返回 `0.8.0.6`，静态资源返回 200；后台未登录访问按权限返回 302。浏览器视觉、iOS Safari、Android Chrome 和微信内置浏览器仍未实测，完整证据见方案最新执行记录。

## 2026-09-05 可转债公告分页、下修排序与到期兑付状态修复（0.8.0.3 已生产发布并验收）

- 深交所批量公告接口实际可能每页返回 50 条，不能以“少于请求的 100 条”判定结束；现取首批后在未覆盖全量时立即切换巨潮备源，避免耗尽任务额度，已补齐希望转2“不下修”等公告事实。
- 下修列表已将“已实施”置于首位；生产接口首位为晶能转债（118034），公告生效日为 2026-09-07；希望转2（127049）已为 `locked`，不再显示为待公告。
- 强赎公告识别新增“到期兑付暨摘牌”；迁移136已落库，南航转债（110075）已通过公告回填恢复，状态为 `announced`/`implementation`，公告日期为 2026-09-03，最后交易日为 2026-10-09，最后转股日为 2026-10-14，公告解析完整。公告增量任务槽位 `484450` 已成功，强赎回填 runId `1906` 发现 46 条、匹配 44 条。
- 0.8.0.3（提交 `608a92d`）已按标准流程生产发布；数据库备份、迁移、Web/Worker/健康检查定时器和公网 `/health` 均验收通过，版本为 `0.8.0.3`。

## 2026-09-05 个股分析任务首次误报失败（0.8.0.4 已生产发布并验收）

- 生产 2026-08-20 至 2026-09-04 的 12 个个股分析计划槽位全部出现“第一次失败、自动重试成功”；第一次运行的 `result_json` 实际为 `ok=true`、`failed=0`，第二次只做数据库新鲜度检查并返回 `fresh`。
- 根因是个股分析成功结果始终带有空数组 `skipped: []`，统一执行器把 JavaScript 空数组误判为跳过信号，进入失败分支并写入 `JOB_FAILED/unknown`，随后创建 5 分钟自动重试。
- 已让执行器只把非空 `skipped` 视为跳过信号，并补充回归断言；提交 `b8fd067` 已生产发布，`/health` 返回 `0.8.0.4`，Web/Worker/健康检查定时器均为 active/enabled，实际个股分析接口返回 200。架构说明和完整复盘见 [`INC-0009`](incidents/INC-0009-个股分析任务成功误报失败.md)。本次未执行生产数据同步。

## 2026-09-05 异地备份、隔离恢复演练与失败告警（0.8.0.0 已生产验收，异地存储待配置）

- 生产已部署提交 `9bbbebf`、版本 `0.8.0.0`；`/health`、`/ready` 均正常，PostgreSQL/Redis 检查通过。systemd 单元校验通过，备份与恢复演练 timer 为 enabled/active。
- 18:38–18:40 生产复跑恢复演练和加密备份：恢复使用 `portfolio_20260905_162730.sql.gz.gpg` 成功，124 个用户对象、135 条迁移记录以及 `users/accounts/positions/trades/nav_history/instruments` 关键表行数均与生产一致，临时库无残留；备份生成新的 `.gpg` 与 `.sha256`，校验通过。
- 上次验收暴露的代码缺口已修复并实测：异地服务固定 `REQUIRE_ENCRYPTION=1`；恢复演练增加关键表行数对账、迁移记录门槛和清理失败检测；备份/恢复成功后旧告警自动变为 `resolved`；将数据库端口切到不可用地址后，告警入口返回 `fallback=smtp`。

- 新增 `scripts/sync_encrypted_backups.sh` 与 `portfolio-db-offsite-sync.service/timer`：先校验 `.gpg.sha256`，只允许把加密备份和校验文件同步到 S3 兼容对象存储，拒绝明文 SQL。生产机已安装单元但未启用 timer，因为当前 SSH 可见环境没有 `aws`/`rclone`、COS 地址或凭据；`/etc/portfolio/backup.env` 的 `BACKUP_OFFSITE_ENABLED=0` 为安全默认，待补齐腾讯云 COS 配置后再启用和做远端对象核验。
- 新增 `scripts/restore_backup_drill.sh` 与 `portfolio-db-restore-drill.service/timer`：每月在固定前缀 `portfolio_restore_drill_YYYYMMDD_HHMMSS` 数据库中恢复，复用 root-only GPG 目录，校验用户对象、`schema_migrations` 及关键业务表行数后自动清理。生产已用 `portfolio_20260905_162730.sql.gz.gpg` 演练通过（124 个用户对象、135 条迁移记录、临时库 0 个残留），未改动 `portfolio` 业务库。
- `portfolio-db-backup.service`、异地同步和恢复演练均挂接 `portfolio-backup-alert@.service`，入口复用 `server/services/jobAlertMailer.js` 的 `ops.alert_notifications` 与 SMTP 重试。生产实测备份和恢复成功后的旧告警均已收敛为 `resolved`，数据库不可用注入返回 `fallback=smtp`。

## 2026-09-05 项目与服务器安全架构审计（首批整改已生产发布，架构项执行中）

- 已核对本地与生产并完成版本 `0.7.0.37` 发布：修复知识列表会话吊销、PATCH 来源检查、API 静态后缀缓存覆盖、AI 配置密钥存储、邮箱改动复核、本人改密限流；Web/Worker systemd 隔离、IPv4/IPv6 端口白名单、应用目录权限收窄和加密备份定时器均已生效。
- `qs`、`@xmldom/xmldom` 已通过 overrides 升级，生产 Python `requests` 已更新到 `2.32.4`；四个 Python 运行时覆盖变量已统一指向受控虚拟环境。全量测试 115 通过、0 失败、0 跳过，`npm audit --omit=dev` 为 0 vulnerabilities。
- 生产已完成本机加密备份、校验和解密完整性验证；本轮已加入异地对象存储同步脚本、隔离数据库恢复演练及失败告警 systemd 单元，并完成生产安装与演练。腾讯云 COS 控制台、对象存储客户端和凭据当前不可见，`BACKUP_OFFSITE_ENABLED` 保持关闭，不能宣称异地副本已生成；待补齐 COS 配置后再启用并做远端对象核验。证据、限制和剩余架构项见 [项目与服务器安全架构审计及整改方案](项目与服务器安全架构审计及整改方案_20260905.md)。
- 本轮新增并已通过本地全量测试：IPO 模型支持 `IPO_MODEL_DIR` 运行目录及幂等迁移脚本；Web/Worker 通过 `DISABLE_RUNTIME_MIGRATIONS=1` 只做 schema 版本检查，迁移使用独立入口和 `portfolio_owner`；CSP 静态内联脚本/样式改用 SHA-256 哈希，AI 请求逐跳解析 DNS、拒绝非公网和跨域跳转并固定已验证 IP。生产执行仍需按部署流程完成模型迁移、角色权限变更和服务验收。
- 腾讯云安全组、WAF、对象存储等控制台配置无法由当前服务器 SSH 或仓库凭据读取，继续标记为未核实，不能用主机防火墙替代云侧结论。
- `.38` 已在生产完成模型运行目录迁移、迁移账号角色拆分和服务验收；`.39` 已删除 Git 中两份历史模型 JSON，生产以 `/var/lib/portfolio/models/ipo` 为唯一模型来源。

## 2026-09-05 HTTP/2 静态资源 429（0.7.0.33 已生产修复并验收）

- 启用 HTTP/2 后，Nginx 的 `limit_conn portfolio_conn 30` 位于 `server` 级，首页约 50 个并发 JS/CSS 请求被计入限制，部分关键脚本返回 429，导致页面无法正常初始化；Web、Worker、数据库和业务接口本身正常。
- 两份 Nginx 模板已将连接限制移入动态 `location /`，静态资源不再受限；生产提交 `9e303b4` 已部署，HTTP/2 单连接并发验收 50/50 个 JS/CSS 均返回 200，修复后新增连接限制错误为 0。完整复盘见 [`INC-0008`](incidents/INC-0008-HTTP2静态资源被连接限制误拦截.md)。

## 2026-09-04 公司财务任务现役可转债范围修复（本地已修复，未生产发布）

- 生产首个日常财务增量槽位错误生成 1,264 家目标，其中包含 19 家港股和大量历史 `bond_unified status='listed'` 对象；现行最新行情日有效可转债正股实际为 308 家，且 2026 年一季报、中报四类报表已完整。
- 根因是日常任务没有复用当前有效可转债筛选，同时标准 `fina_indicator` 的无 `report_type` 响应被误过滤，80 次正常批次边界又被当作失败重试。
- 本地已把日常同步、人工登记和历史回填统一限制为最新行情日仍上市、未到期、未退市、非定向/私募的可转债正股；日常只按最近两个报告期和真实披露计划增量处理，正常剩余批次返回 `partial`，失败游标在退避期不再堵塞队首。完整根因和发布验收见 [`INC-0007`](incidents/INC-0007-公司财务任务重复纳入历史退市债.md)。

## 2026-09-04 港币汇率预算等待误熔断（0.7.0.28 已生产发布并验收）

- 生产汇率策略收紧后，页面每 15 分钟刷新和持仓对比触发内部 `BUDGET_WAIT`；旧汇率适配器把该等待误写成无恢复时间的来源熔断，导致 `/api/hkrate` 500、后台计划 53 次无效重试。最近有效汇率 `0.859029` 未被错误覆盖。
- 修复 `hkRate.js` 的错误分类：内部预算等待只进入调度等待，真实上游 429/额度错误才按接口范围和恢复时间熔断；`/api/hkrate` 与持仓对比失败时回退最近有效汇率。迁移135把最小间隔统一为24小时，并关闭本次误开的历史熔断。
- 新增汇率回归检查，覆盖任务、路由、持仓对比和存量熔断迁移。完整根因、漏检原因、生产证据和遗留事项见 [`INC-0006`](incidents/INC-0006-港币汇率预算等待误熔断.md)。
- 生产提交 `12fe5b5` 已部署；迁移135执行成功，旧失败槽位 `442118` 自动恢复并以 1 次外部调用成功，最新汇率 `0.859096` 已同步 10 个账户；Web/Worker/健康检查和两个公网健康地址均通过，汇率来源开放熔断数为 0。

## 2026-09-04 可转债安全性财务数据增量同步整改（0.7.0.27 已生产执行并验收）

- 已执行迁移 `134_company_financial_incremental_sync`：财务标准报告补齐 `f_ann_date`、`report_type`、`comp_type`，安全性快照增加 `publication_status`、质量门禁结果和拒绝原因；历史全量未评级快照标记为 `rejected`，上一份有效快照保持 `published`。
- 已注册 `company_financial_incremental_sync` 每日 06:30 自然日任务和 `stock_financial_reports` 数据分区，新增公司并集、披露期探测、缺口/更正/重试队列、公司级游标、原始报文审计、`report_type=1` 合并报表约束和内容哈希版本；新增 `server/scripts/backfillCompanyFinancials.js`，默认只预览，应用模式按报告期缺口续跑。
- 安全性快照改为发布前质量门禁并只由任务编排登记分区，失败保留上一份有效快照；股票分析定时、手动刷新和新增自选后的刷新均只读标准层，财务缺口只登记增量队列。
- 生产执行记录：版本 `0.7.0.27`（提交 `fee70b8`）已完成迁移134、Web/Worker/健康检查部署；主 Token 四个财务 VIP 接口权限探测 `4/4` 可用。按最新安全性行情范围回填 `308` 家正股、报告期 `2026-03-31` 和 `2026-06-30`，两期四类报表完整覆盖 `308/308`；安全性快照 `id=50` 发布 `311` 条，质量门禁通过（财务覆盖率100%、有评级覆盖率100%），数据分区 `2026-09-03` 已发布且未过期。公开接口当前返回 `310` 条，是因为自动过滤了1条非活跃转债。实现细节以 [技术架构](技术架构.md)、[任务接口数据集矩阵](任务接口数据集矩阵.generated.md) 和整改方案为准。

## 2026-09-03 可转债不下修事实时间线防复发（0.7.0.23 已生产发布）

- 根因：`123119` 的最新“不向下修正”公告早于当前数据日，但视图先命中旧的“提议下修”事件；同时旧 v6 解析器把没有固定锁定期限的公告写成债券到期日，导致数学计数没有从公告后的首个交易日重新开始。
- 修复：视图按不下修公告与修正事件的公告日期决定优先级；无固定锁定终点的不下修事实不再使用到期日，公告给出明确下一轮起算日时保存该日期，否则置空并从公告后的首个交易日重新起算；旧解析器的“未来日期＋未声明锁定”进入库内官方 PDF 的 v7 有界重解析；接口增加 `decision_status_conflicts` 质量计数，发现事实冲突时标记质量异常。
- 生产复核：`0.7.0.22` 已执行迁移 `132_convertible_bond_revision_decision_timeline` 与 `133_convertible_bond_revision_legacy_false_facts`，但康泰转2仍残留 v7 到期日锁定事实；`0.7.0.23`（提交 `407eb4e`）新增启动补漏条件，命中 v7 的 `next_eligible_date/valid_until` 等于债券到期日时进入有界重解析。生产已完成 10 只缓存公告补漏、3 只产生变化、310 只下修结果重算；康泰转2恢复为 `tracking`、`6/15`，迁移与质量检查通过，公网接口已验证。当前最新行情日为 `2026-09-02`，接口因等待 `2026-09-03` 行情分区而标记 `stale=true`，不影响本次事实重解析结果。

## 2026-09-03 打新历史详情入口（已生产发布并完成线上验收）

- 新股历史表的“预测涨幅”在存在预测值和证券代码时可点击，打开对应 `ipo-report.html?code=证券代码` 打新详情；无历史预测或缺少代码时仍显示原文本。
- 前端资源版本更新为 `ipo.js?v=14`，回归测试覆盖链接代码、预测值、打开方式和无预测分支；版本 `0.7.0.21`（提交 `9a57bcb`）已完成生产部署，Web、Worker、健康检查定时器均正常，线上首页、打新历史接口、详情页和详情接口均返回成功。

## 2026-09-03 打新风口与下游业务暴露校准（已生产发布并完成首批同步）

- 贝特利（`301697`）复盘显示，旧模型把“新材料”直接乘以 `2.68`，把基准预测约 `308%` 放大到约 `825%`；实际首日涨幅约 `196.69%`，赛道放大项贡献了主要误差。相关报告与历史数据见 [IPO 报告接口](https://cunzaitouzi.cn/api/ipo/report?date=20260901) 和 [IPO 历史接口](https://cunzaitouzi.cn/api/ipo/history?type=stock&limit=200)。
- `ipo_lib_sector.py` 保留历史风口表，但改用近365天、剔除北交所后的同周期相对表现和稳健中位数；样本按 `n/(n+5)` 收缩，最终赛道乘数限制在 `0.80～1.50`。少于5只样本不再清零，但会自动折扣。
- 新增 `business_exposure`：从招股书主营业务、产品/应用/客户证据识别光伏、PCB、消费电子、半导体、汽车电子、储能等下游，支持多下游按权重合成；证据不足时保留低置信度和行业兜底，不再把无法可靠拆分的公司直接降为0。预测同时保存基准值、赛道调整点数、乘数、置信度和拆分上下文。
- 迁移 `131_ipo_sector_exposure_calibration` 已在生产执行；版本 `0.7.0.19`（提交 `bfbcb87`）已发布，Web、Worker、健康检查定时器均正常。首批生产同步核心阶段因当天已成功而跳过，19:30补全两批共尝试20条、更新14条，首日表现补回2条，外部调用94次且无备用源切换；贝特利（`301697`）已落库5个下游暴露、可信度0.78。日报任务随后成功生成 `20260904`，外部调用0次并完成新规则校准；仍有历史暴露缺口按每日10条增量补全。全量测试 `npm.cmd run test:all` 为113通过、0失败、0跳过，`npm.cmd run check:knowledge` 通过。

## 2026-09-03 Tushare 双账号与历史直连整改（已修复并完成生产验收）

- 新增迁移128～130：主 Tushare 账号按 6000 积分配置 450 次/分钟、无内部日总量；备用账号按 2000 积分配置 180 次/分钟，并以官方单接口每日 100000 次为依据设置 90000 次凭据级止损；迁移130为四个财务VIP备用策略补齐180次/分钟兜底。官方频率/行数/最低积分写入接口策略，不能再用历史峰值倒推限额。
- 路由按官方积分门槛预筛选：`index_dailybasic`（4000 积分）、`top10_cb_holders`（5000 积分）和四个财务 VIP 接口（5000 积分）不发给 2000 积分备用账号，其余接口仍以具体权限探测结果为准；权限拒绝不再把接口错误扩大成整套 Token 失效。权限拒绝的策略在换 Token 或探测恢复成功后自动重新启用。
- 巨潮资讯既有 20 次/分钟、500 次/日来源级保护原样保留；汇率接口按官方“每日更新、建议24小时请求一次”设置 1 次/分钟、24 次/日和最小间隔，不再沿用通用兜底值。
- `backfill_bond_shd.py`、公告 PDF 解析脚本、3个历史回填脚本以及行情路由的新浪/腾讯直连均已接入统一 Guard；深交所静态 PDF 地址统一计入 `szse`，历史任务不能再通过子进程或旧 HTTP 入口绕过来源、接口和凭据计数。
- 针对性验收：Node 路由/权限恢复、迁移128～130 VIP矩阵、Python 来源映射和历史脚本 Guard 检查均通过；全量 `npm.cmd run test:all` 113 通过、0 失败、0 跳过。生产 0.7.0.17 已核对迁移登记、Web/Worker/健康检查定时器和运行策略，版本健康检查通过。

## 2026-09-03 人工强制补跑参数保留修复

- 生产补数时发现，失败槽位通过管理员重试设置的 `force=true` 会在调度器同步同一计划槽位时被定时参数覆盖，导致任务只返回 `fresh` 而未真正重拉外部数据。
- 本地修复 `ensureSlot`：处于 `manual_retry`/`auto_retry` 且尚未结束的槽位保留原请求参数，并增加回归测试；不改变额度保护、任务上限或数据写入逻辑。
- 本次修复需按标准流程发布后，验证强制补跑标记能够穿过调度同步并由 Worker 实际执行。

## 2026-09-03 后台任务批次上限复核（本次发布）

- 生产 2026-09-03 可转债主同步在完成主数据写入后，执行 90 天周期/正股补漏时被任务自身的 16 次单批上限拦截；当日 Tushare 来源日计数 36/5000、分钟窗口 4/450，确认不是上游额度耗尽。
- 本地取消不合理的过低任务边界：可转债主链 `16→600`（生产历史峰值 281）、指数补齐 `2→10`（双账户五指数）、LOF/ETF 收盘 `16→32`（生产完整补取 30 个腾讯批次）、IPO 核心 `1→3`（允许一次失败后的跨尝试重试）；套利 `12→600` 已随本次未提交改动纳入发布。
- 保留来源级 Tushare 主账号 `450次/分钟、无内部日总量`、备用账号 `180次/分钟、90000次凭据级日止损`，巨潮 `20次/分钟、500次/日` 保护；保留安全评分/日报等数据库只读任务的 0 次约束，以及下修动机输入的 10 次分批边界。任务批次边界只负责单批止损，不替代来源/接口级预算。

## 2026-09-02 晚间后台告警复核（本地已修复，未生产发布）

- 生产 `0.7.0.13` 只读复核当前 4 条待处理告警：套利任务自有 12 次上限过低、IPO 旧等待告警、后续成功后仍残留的打新日报阻断告警、指数当天水位验收过严；Tushare/CNInfo/港交所全局额度和 Worker 状态正常。
- 本地局部修复：套利单批上限调整为 600；指数补齐按最近完整交易日验收；同业务日同阶段已有后续成功实例时，旧失败/等待/阻断/降级槽位自动标记 `skipped` 并关闭告警；`/api/admin/jobs/alerts` 默认只返回待处理项。
- 本轮未执行生产迁移、告警清理、人工补数或部署；生产处理需按部署流程单独授权。

## 2026-09-02 来源＋接口＋凭据策略与精确限流（本地已实现，未生产发布）

- 迁移127新增 `ops.source_endpoint_policies` 和 `ops.source_endpoint_runtime`，并把 `ops.external_call_budgets` 主键扩展为“来源＋接口＋凭据指纹＋窗口”。策略保存官方权限/积分、官方与内部分钟/日限额、并发、最小间隔、超时、重试、空结果和启用状态；Token 明文仍只保存在加密配置，策略表仅保存角色与不可逆指纹。
- Node `externalCallGuard.js`、Python `ipo-report/external_call_guard.py` 统一调用 `ops.reserve_external_call()`，按接口计数并累计同一凭据跨接口用量；接口并发槽和最小间隔在请求完成后释放。主/备 Tushare 共享来源但按凭据指纹隔离，Token 轮换自动刷新指纹并重置权限核验。
- 后台外部 API 设置页新增接口策略面板及保存接口，权限探测结果会回写具体接口/凭据策略；未配置或明确无权限的策略直接阻断，分钟/日/并发/间隔达到保护线进入等待。
- 本地验收新增 `server/test/source-endpoint-policy.test.js`，并更新 Node/Python 与主备路由回归夹具；定向测试通过。当前只完成本地迁移与代码验证，未执行生产迁移、配置同步、数据同步或部署。

## 2026-09-02 受限额度全面复盘（0.7.0.13 已生产发布并完成线上验证）

- 生产只读核查确认：主 Tushare 当日 `100/100` 是系统内部日保护线，不是官方 6,000 积分额度耗尽；达到上限前实际仅使用 17 个分钟窗口、单窗口最高 16 次，打新核心请求因此在发出前被拦截，随后日报因上游事实未成功而阻断。官方 5,000 积分以上常规接口为 500 次/分钟，常规数据无每日总量上限，详见 [Tushare 接口权限](https://tushare.pro/document/1?doc_id=108) 与 [接口调用频率](https://tushare.pro/document/1?doc_id=290)。
- 额度来源已厘清：`ops.external_call_budgets` 是按来源、上海自然日和分钟窗口累计的系统保护计数；任务契约里的 `dailyBudget` 只是每日预算报表，`maxExternalCallsPerRun` 只是单批边界，三者不能互相代替。原来的 80/100 方案是旧的内部止血目标，不是 Tushare 官方配额。
- 本地已把 Node/Python 两端默认保护线统一调整为：主、备用 Tushare 均为 `450 次/分钟、5000 次/日`，巨潮资讯 `20 次/分钟、500 次/日`；当前实际使用的腾讯行情不设置本系统分钟/日预算，仅保留上游异常处理。汇率、中债、港交所、公告/风控等其他经过 Guard 的来源仍使用通用 `60 次/分钟、2000 次/日` 兜底。5000 次/日是按生产近期峰值 1397 次加两倍以上余量设定的内部保护线，不代表上游日限额；旧 `.env` 预算变量仅作兼容读取，不覆盖已落库策略。
- 单接口限制已在本地通过迁移127完成：`ops.source_endpoint_policies` 保存接口规则，预算计数和原子扣减函数已扩展到“来源＋接口＋凭据”维度，避免出现“表里有配置、运行时不执行”的假控制；生产迁移仍需单独授权。
- 本轮没有修改任务批次边界：IPO 核心仍为 1 次 `new_share`、晚间补全 15 次，打新日报仍为 0 次外部调用；这样解决来源日额度误伤，同时保留批次止损和断点续跑。额度调整已随 `0.7.0.13` 发布，未清理既有生产计数。

## 2026-09-02 统一调度器日期类型兼容修复（已修复并完成生产验证）

- 生产复核发现 Worker 服务虽为 `active`，但统一调度器每分钟因 `invalid input syntax for type date: "Wed Sep 02"` 失败，导致手工入队的 IPO 核心任务无法领取。
- 根因是 PostgreSQL `date` 字段在 Node 侧返回 JavaScript `Date`，数据分区依赖校验直接执行 `String(slot.business_date).slice(0, 10)`，把日期截成英文日期前缀。
- 修复为统一经过 `normalizeBusinessDate()` 转换并增加回归约束；不改变任务、额度、数据表和重试策略。完整测试 `portfolio_test` 为 112 通过、0 失败、0 跳过。
- 版本 `0.7.0.13`、提交 `48186a4` 已完成生产部署；Web、Worker、健康检查定时器均 `active`，`/health` 返回版本一致。补齐交易日历至 `2026-09-30` 后，IPO 核心运行记录 `2072` 成功（Tushare 1 次），`ipo_history` 分区质量 `passed`；打新日历运行记录 `2073` 成功（外部调用 0 次），生成 `2026-09-03` 日报（新股 1 只、新债 1 只），日历/日报 API 和页面均返回 200，部署后未再出现日期错误。

## 2026-09-02 收盘基金/ETF与安全评分人工失败修复（0.7.0.11 已生产发布，跟踪中）

- 生产核查确认截图中的 3 条失败已自行恢复：LOF/ETF 计划 `383581` 第 3 次在 16:28 成功、6 条价格已落库；安全评分计划 `397522` 的人工补跑失败后，14:20 正式计划成功生成 `2026-09-01` 快照。6 条基金/ETF 收盘记录对应 `160719`、`160723`（两个账户）、`161226`、`510900`、`511880`，不是腾讯无报价或主档仍冲突。
- 根因与修复：批量行情缺失时旧逻辑重复读取同一份不完整 Map，并错误累计本地检查次数；现改为缺失代码一次有界批量补取，且 `externalCalls` 只取统一 Guard 实际请求数。安全评分任务已声明外部调用为 0，但人工入口仍走外部模式；现自动、人工和后台刷新统一只读共享标准层。详见 [`INC-0005-收盘基金ETF与安全评分人工入口失败.md`](incidents/INC-0005-收盘基金ETF与安全评分人工入口失败.md) 与 [`技术架构.md`](技术架构.md)。
- 回归：收盘 13 项、安全评分 22 项、任务编排 95 项通过；全量测试测试库 `portfolio_test` 112 项通过。生产已发布 `0.7.0.11`，需继续观察 3 个交易日的完整率和实际调用数。

## 2026-09-02 退市债固定价格归因修复（本地已实现）

- 根因：华泰账户的搜特退债（`404002`）已退市，行情源按规则不返回报价；虽然持仓保留了最后退市价 `2.362`，净值归因仍因目标日没有 `daily_prices` 把它判为缺失，导致整段归因不完整。
- 修复：`server/services/navAttribution.js` 对代码或名称明确属于退市债的标的，在没有有效日行情时使用持仓/快照/最后成交中保留的正数价格作为固定退市价，并返回 `manualPriceCodes` 留痕；普通证券缺行情仍严格标记不完整。
- 核查：本地华泰账户归因由 `complete=false、missingCodes=[404002]` 修复为 `complete=true、missingCodes=[]、manualPriceCodes=[404002]`，快照残差约为 0；新增退市债归因回归测试。技术口径已同步至 [`技术架构.md`](技术架构.md) 9.1。

## 2026-09-02 场内基金/ETF 行情缺失（已完成生产发布）

- 根因：历史持仓中的 `160719`、`161226`、`511880` 不在 `stock_basic` 统一主档；旧代码把 15/16 开头基金误判为沪市、把 51 开头 ETF 的标准后缀推成 `.SZ`，统一腾讯映射解析失败后页面只显示旧价格和涨跌幅 `--`。腾讯源实测分别返回 `sz160719`、`sz161226`、`sh511880`。
- 修复：统一分类改为沪市 `5xxxxx`、深市 `15/16/17xxxx`、深市 REITs `180101–180999` 与商业不动产 REITs `181001–181999`、深市封闭式基金 `184xxx`；标准代码映射同步校正，20xxxx 深市 B 股、900xxx 沪市 B 股和 92xxxx 北交所股票也纳入同一规则。腾讯行情遇历史基金缺主档时先由统一身份写入口补齐最小主档和腾讯映射，数据库暂不可用才允许本次按同一规则直连。批量与单只入口均已验证返回价格、涨跌幅和行情时间。
- 官方依据已记入 [`技术架构.md`](技术架构.md)：[深交所证券代码区间表（2024 年 4 月修订）](https://www.szse.cn/marketServices/technicalservice/doc/P020240510623040090566.pdf)、[深交所商业不动产 REITs 代码段通知（2026-03-06）](https://investor.szse.cn/marketServices/technicalservice/notice/t20260306_619323.html)、[上交所 ETF 代码段扩展（2026-08-31）](https://www.sse.com.cn/lawandrules/guide/stock/jyglywznylc/zn/c/c_20260831_10830435.shtml)、[上交所 ETF 代码段扩展（2026-07-13）](https://big5.sse.com.cn/site/cht/www.sse.com.cn/lawandrules/guide/stock/jyglywznylc/zn/c/c_20260713_10825354.shtml)、[北交所代码编制指引](https://www.bse.cn/jygl_list/200011019.html)。
- 验收：全量核查当前持仓 6 只场内基金/ETF（160719、160723、161226、501312、510900、511880）全部返回价格、涨跌幅和行情时间；全量测试 112 通过、0 失败、0 跳过；`check:knowledge` 通过。版本 `0.7.0.8`、提交 `4347c77` 已推送并完成生产部署；生产 Web、Worker、健康检查定时器均 `active/enabled`，生产 `/health` 返回 `0.7.0.8`，`cunzaitouzi.cn` 与 `www.cunzaitouzi.cn` 均返回 200 并加载 `code-classify.js?v=5`。本次仅发布代码和文档，未执行生产数据同步。

## 2026-09-02 整改后告警复盘与统一修复

- 早间生产开放告警 10 条实际收敛为约 6 个计划实例问题，根因为预算日键被业务日期污染、Node/Python 跨进程调用计数未延续、任务单次上限与每日预算混用、安全评分早于共享主链，以及同一计划告警未归并；接口权限限制按 Tushare API 区分，不能把单个 API 失败解释为整套 Token 受限。
- 本地已完成修复：预算按上海实际请求日计数；Python 错误结构化返回并由 Node 传递累计调用数；`JOB_BUDGET_EXCEEDED` 不再自动重试；批量任务单次上限与每日计入值分离（每日计入合计仍为 72）；安全评分改为 08:30 并依赖 08:00 主链；同槽位告警归并；下修动机批次边界保留游标并顺延。详见 [`INC-0004-整改后预算与定时计划告警.md`](incidents/INC-0004-整改后预算与定时计划告警.md)。
- 已按授权发布版本 `0.7.0.7`（提交 `2a4ca5d`）并完成生产补跑：安全评分槽位 `397522` 生成快照47，来源日期与分区均为 `2026-09-01`；强赎公告槽位 `383588` 成功发布252条数据。真正开放告警由8条降至3条，剩余为 IPO/套利受预算顺延的历史批次，暂不强行重试；后续按专项方案连续观察3个交易日。

## 2026-09-02 持仓行情缺失与历史证券主档收敛（已完成生产执行）

- 已确认根因：统一身份解析遇到历史裸代码/错误后缀重复主档时判定歧义，腾讯实时行情被静默丢弃，持仓页因此沿用旧价格并显示涨跌幅 `--`；不是腾讯上游无报价，也不是统一取数架构本身冲突。
- 按用户授权完成版本 `0.7.0.6`（提交 `1526551`）生产发布。备份 `/var/backups/portfolio_before_0_7_0_6_suffix_validation_20260902_133700.dump`，SHA-256 `970b05074893686fd720841f6d09a2e9e57fd3e7ed346a2b04fa45111662b4a3`。迁移125/126在单事务内自动归并并物理删除历史主档、迁移外键、清理 `nan/HTML` 无效主档、收敛公司关系、清空候选记录并校验后缀冲突，不需要人工逐条处理；生产重复标准身份、候选、无效代码、非标准市场代码、重复发行公司关系均为0。
- 发现的7组北交所错误后缀（包括 `.SH.SH`、误标 `.SH/.SZ`）已统一为 `.BJ`；数据库唯一索引已建立。`920002.SH→920002.BJ`、`600519.BJ→600519.SH` 等校验通过。生产 Web、Worker、健康检查定时器均 `active/enabled`，健康版本 `0.7.0.6`。统一批量行情抽查 `601919`、`601198`、`123175` 均返回价格、涨跌幅和行情时间；持仓数量与交易数量未被迁移改写（当前分别为72、98）。
- 全量测试最终结果为 112 通过、0 失败、0 跳过。后续仅需按 incident 文档连续3个交易日观察行情完整率、批量请求量、净值快照和告警，不再执行人工主档处理。

## 2026-09-01 证券数据架构、自动任务与 API 调用保护专项整改方案（首批已落地，分批执行中）

- 本轮讨论已统一整理进既有专项文档：[`自动任务重复执行与API调用保护整改方案_20260815.md`](自动任务重复执行与API调用保护整改方案_20260815.md)，没有另建重复方案。原第 1～10 节保留 2026-08-15 初版背景，第 11～22 节为当前整改依据；内容冲突时以后续章节为准。
- 当前专项范围已经扩展为：统一公司/证券身份和多数据源代码映射、股票基础资料与行情及财务三表标准层、可转债按 `stock_instrument_id` 关联正股、6,000 积分 Tushare 批量采集、共享采集任务、平滑限速、接口级熔断、主备 Token 隔离和数据分区发布。
- 评审后已明确区分“采集止血”和“数据架构治理”：现有23个任务定义（20个定时、3个人工）的任务契约和基础调度能力不重复开发；常规自动 Tushare 调用目标不超过80次/交易日、硬上限100次，核心主档、收盘和停牌分区不超过10次；`ops.dataset_partitions` 前置到止血批次，历史证券ID合并推迟到最后。
- Python Guard 开启后已与 Node 共用预算表和数据库锁，整改重点是统一两端策略语义并禁止生产脚本无保护运行，而不是再分配一套独立额度；证券身份与权限探测分别改造现有 `securityIdentity.js`、`externalApiConfig.js`，禁止新增平行实现。
- 2026-09-01 补强四项必做：运行时所有供应商代码转换强制读取 `instrument_identifiers` 并使用有上限的内存缓存；Python Guard 改为默认开启；`suspend_d` 从估值计算移入 P1 共享采集；任务/接口/数据集/预算矩阵从 `JOB_DEFINITIONS` 自动生成并由 `check:knowledge` 校验。
- 本轮已执行 P0/P1/P2/P3 的可逆部分：Node/Python 外部预算语义统一为窗口等待（`BUDGET_WAIT`），预算扣减统一调用 `ops.consume_external_call_budget()` 数据库原子函数，Python Guard 默认开启且生产不可关闭；`suspend_d` 从估值链路移入可转债主采集链路；共享收盘批次用一次 `stock_basic` 建立全A股标准主档，并一次写入 `daily`、`daily_basic`、`adj_factor` 全市场分区；运行时供应商代码解析接入 Node/Python 两套身份服务的有界缓存，腾讯/东方财富/新浪/雪球代码缺失时由身份模块先落映射并校验，业务层不再手工拼接；并为指数补齐标准身份和映射。迁移119～124已完成股息率单位修复、分区水位初始化、历史 `equity`→`stock` 归一和公司关系补齐。任务矩阵由 [`任务接口数据集矩阵.generated.md`](任务接口数据集矩阵.generated.md) 自动生成，`check:knowledge` 会校验是否漂移。新增 [`server/scripts/acceptanceDataArchitecture.js`](../server/scripts/acceptanceDataArchitecture.js) 与 `npm.cmd run check:data-architecture` 提供不调用外部接口的只读验收。新增迁移118和 [`server/scripts/auditInstrumentMerges.js`](../server/scripts/auditInstrumentMerges.js) 已完成只读审计/候选留痕：本地发现4,538组候选（4,367组名称一致、171组需人工核对、当前外键引用4,810行；审计已排除候选表自身的记录），没有执行物理合并或删除。Tushare 权限探测入口为 [`server/scripts/probeTusharePermissions.js`](../server/scripts/probeTusharePermissions.js)，已扩展 `income_vip`、`balancesheet_vip`、`cashflow_vip`、`fina_indicator_vip`，探测本身受统一预算和熔断保护。自动验收：全量测试111通过、0失败、0跳过；本地最新估值股息率异常0条、已发布分区空分区0条、有效行情转债缺正股关联0条、股票 `equity` 0条。仍有6条历史股票主档无名称无法补公司关系、4,686条历史裸股票代码（139条可直接规范化、4,547条需合并）及185条旧测试熔断记录；生产切换、历史重建和旧ID物理合并仍需按专项分阶段授权。
- 2026-09-01 已按授权完成生产切换与维护窗口可逆处理：生产提交 `c989bfb`、版本 `0.7.0.0`，部署前备份 `/var/backups/portfolio_before_0_7_0_0_20260901_191425.dump`（SHA-256 `ef153e23cf879f5d803c4923e2e428553b626b2c56849b614357e91932d38836`）。Tushare 主/备 Token 权限探测 48 项中 18 项可用或空结果可接受；主 Token 24 项均未发出上游请求（多数被统一 Guard 当日预算保护拦截），备用 Token 4 个 `_vip` 接口无权限，另有 2 个接口受限。生产已规范化 139 条裸股票代码，并生成 4,789 组历史 ID 合并候选（4,624 组名称一致、165 组需人工核对、9,350 行引用）；未做物理合并/删除，生产旧 `test_guard` 熔断记录为 0。影子对账从下一交易日开始，须连续 3 个交易日核验；部署前最后一次共享收盘任务为 281 次 Tushare 调用，观察期未完成前维持 `pass_with_controlled_pending`。
- 2026-09-01 版本 `0.7.0.1`（提交 `a56eb12`）按授权完成生产维护窗口：备份 `/var/backups/portfolio_before_id_partition_0_7_0_1_20260901_201106.dump`（SHA-256 `e50c32ea9f3856be91d0807e2410d3bd0cc95c32303eda8319ea08c2ac3ad72d`）。历史 ID 安全组迁移 4,537 个旧主档、更新 217 条外键引用、去重 4,537 条确定重复公司关系；旧主档保留并标记 `merged`，迁移后旧 ID 外键引用 0。剩余 169 条候选待处理（165 条人工冲突、4 条无明确标准目标）。17 个非核心数据集已回填并发布分区，生产共 23/23 个任务产出数据集有分区、空发布 0；Web、Worker、健康检查定时器均 active，健康版本 `0.7.0.1`。影子对账仍需连续 3 个交易日观察。
- 已建立后续观察安排：从 2026-09-02 起按 `market.trade_calendar` 识别上海交易日，连续 3 个交易日记录共享采集与部署前 281 次 Tushare 调用基线的差异；主 Token 权限探测仅在首个符合条件的自然日执行一次，固定走 Guard 和数据库预算，不切备用、不绕过预算、不循环重试。
- 2026-09-02 首个观察窗口已留痕：生产当前健康版本为 `0.7.0.4`（提交 `4fed117`，后续外部发布版本）。共享任务最近两次运行分别为 `job_runs.id=1943` 成功且外部调用 0 次、`job_runs.id=1933` 失败且 Tushare 调用 8 次，均低于 281 次基线。主 Token 权限探测 24/24 接口均在 Guard 预检阶段被当日预算 `100/100` 拦截，未发起上游请求、未切备用、未重试，权限结论保持待定；`cb_price_chg` 既有熔断为 `PERMISSION_DENIED` 且无探测租约残留。因 `market.trade_calendar` 尚无 2026-09-02 记录，本日不计入连续 3 个已确认交易日，观察期继续保持 `pass_with_controlled_pending`。
- IPO链路已按统一任务架构收敛（业务实现版本 `0.7.0.2`、提交 `54fdf89`）并完成生产部署：保留 `ipo_history_sync` 任务代码，18:00作为 `new_share` 唯一采集者并发布带集合/批次/缺失诊断的 `ipo_history` 当天分区，19:30仅补非紧急字段；`ipo_calendar_refresh` 调整为18:05，依赖当天质量通过的已发布分区，日报只读标准事实并在发布前做证券集合对账。不一致时保留上一份有效日报并告警。分阶段预算为核心1次、晚间补全15次、日报0次，任务矩阵每日声明总额72次；隔离测试库全量测试111通过、0失败、0跳过，生产 Web、Worker、健康检查定时器均 `active/enabled`，代码部署验收时健康版本和变更记录为 `0.7.0.2`，IPO日历/历史接口返回200；本次 `0.7.0.3` 仅同步发布元数据，不改业务代码。部署当日 Tushare 自动任务已达到100次硬上限，18:00核心阶段处于 `waiting_external`、18:05日报按门禁 `blocked` 并保留旧结果，19:30非紧急补全成功；未执行人工生产补数。

## 2026-08-31 下修动机详情可读性与正股财务缓存修复（本地已实现）

- 详情页“查看计算项”默认展开；前十大持有人、控制人不再直接显示原始 JSON，改为中文明细；历史下修按“第1轮、第2轮……”逐轮换行；移除“市场每月提议下修次数”输入项，市场热度仍只作为背景，不进入单券评分。
- 正股财务取数补齐 `bond_safety_financial_cache` 的实际字段映射（`cash`、`total_liability` 等），并在详情审计来源中标注为“已入库正股财务缓存”。本地 2026-08-28 候选核查：309 只中 290 只有财务缓存，290 只可读到账上现金和负债，228 只有资产合计；其余没有可用缓存的正股仍需后续数据补齐，页面明确显示暂无数据。

## 2026-08-31 下修动机研究等级与详情入口修复（本地已完成）

- 下修列表的“动机等级”不再因 `unavailable`、`partial` 或已发生下修事实而取消链接；当前列表中的每只转债都能进入下修博弈详情，没有评分快照时详情页也会明确说明等待计算。
- 回测未通过仍禁止输出“强动机/存在动机”等预测结论；列表改为按同一评分日、同一模型的全市场分位展示非预测性的“研究等级较高/中等/较低”，其中前20%为较高。本地重算 309/309 成功、0失败，分布为较高64只、中等149只、较低96只；数据质量为73只 `complete`、236只 `partial`。
- 修复数据质量误判：Tushare `top10_cb_holders` 不提供公告日，历史取数改用年报4月30日、中报8月31日等法定最晚披露日作为保守可见日；`pledge_stat` 正常空结果使用成功游标标记为“已核验未返回记录”，不再与接口失败混淆。
- 修复回测收益口径：上线门槛中的扣成本收益只统计高分组，不再误用全部样本平均收益。修复后重跑仍为364个样本、156个样本外样本、0个70分以上样本、关键输入完整率0%，高分组收益因此为无样本而非原先的全样本负收益；`motive-v1.1` 预测门禁保持关闭。

## 2026-08-31 下修动机评分生产发布与验收（已完成）

- 版本 `0.6.5.7` 的代码发布提交 `61795e6` 已推送 `master`，并按标准生产脚本完成部署；部署时自动执行迁移 115，`analytics.convertible_bond_revision_motive_backtests` 已在生产存在。
- 生产 Web、Worker、健康检查定时器均为 `active/enabled`；健康检查版本为 `0.6.5.7`，就绪检查中的数据库与 Redis 均通过。下修接口返回 `stale=false`、公告质量正常，当前 309 只候选中 5 只已有下修事实、0 只被标为 `observed_weak`，309 只动机等级为 `unavailable`，符合回测未达标时的安全策略。
- 生产页面资源版本为下修列表 `45`、动机详情 `54`；该条是版本 `0.6.5.7` 的历史验收记录，后续本地修复的目标资源版本为下修列表 `46`、动机详情 `55`，未部署前不得视为线上现役。
- 本次仅发布代码与数据库迁移，未执行生产历史回填或主动输入同步。部署后调度器重试了 2026-08-29 的既有动机输入同步计划，该计划因 Tushare `top10_cb_holders` 及备用接口失败而保持 `degraded`，属于数据源/额度问题，不是本次发布失败；后续补数需单独授权。

## 2026-08-31 下修动机历史回填与回测验收（本地已执行，模型保持关闭）

- 新增迁移 115 与 `analytics.convertible_bond_revision_motive_backtests`，回测入口为 `server/scripts/backtestConvertibleBondMotive.js`；支持公告前时点取数、2025 年样本外窗口、0.30% 往返成本和历史评分快照幂等回填。
- 本地已回填 364 个历史触发周期评分，覆盖 2022-08-01 至 2025-12-31；样本外 156 个周期、34 个提议下修，关键输入完整率 0%，70 分以上组 0 个，扣成本平均收益 -0.1943%，回测结果明确未达上线门槛。
- 因回测未达标，`motive-v1.1` 继续输出研究分但统一为 `motive_level=unavailable`；已提议/已实施等只显示公告事实，不再显示“动机偏弱”预测。持有人同步已成功写入部分历史事实（本地63只、4,730条），每批最多10次；正常空结果推进游标，真正权限错误保留失败游标，达到调用上限或接口停止后未实际请求的对象不推进游标，不以空数据冒充完整。
- 证据：`server/test/convertible-bond-motive-backtest.test.js`、`npm.cmd run backtest:motive`（结果 pass=false 为预期安全门禁）、`npm.cmd run test:all`、`npm.cmd run check:knowledge`。生产发布和生产数据同步仍需按生产流程取得授权。

## 2026-08-31 下修动机生产补数与空结果排序（已发布）

- 版本 `0.6.5.5`、提交 `f2f111c` 已发布生产：同步候选继续按当前行情过滤；已尝试但为空的持有人/质押对象按 `last_attempt_at` 后置，避免批次反复卡在同一批，同时保留后续复查机会。
- 已获用户授权并完成 2026-08-28 生产输入补数：当前 309 只候选中 258 只具备 `top10_cb_holders` 持有人事实（全库 276 只、17,200 行）；`pledge_stat` 共 2 家、均为 2026-08-28 快照，分别对应 000001.SZ、600690.SH，不属于当前 309 只候选。其余 51 只经过多轮正常空结果复查，未覆盖对象以 Tushare 空数据留痕，不覆盖旧事实。
- 309/309 只已重算 `motive-v1` 评分、0 计算失败；当前 `quality_status=partial`、完整率 0.666667，表示部分上游输入缺失，不表示评分任务失败。生产 `/api/bond-revision` 已返回 `stale=false`、`quality.status=ok`；Web、Worker、健康检查定时器均 active/enabled。
- Tushare `top10_cb_holders` 的 30 次/分钟限频由接口级熔断和每批最多 10 只保护；备用 Token 对该接口无权限，本次补数强制主 Token 后恢复自动模式。后续由 Worker 按 72 小时周末补偿调度继续复查。

## 2026-08-31 下修动机评分止血修正（本地已实现，回测未达标）

- `motive-v1.1` 删除“剩余规模低于发行规模”的无意义加分；“剩余规模/发行规模”仅保留为未转股比例，在达到70%或90%时计入促转股压力。
- 全市场及同行其他转债的下修提议不再计入单债动机分，仅作为详情页市场背景，避免把没有可靠因果说明的相关性当成发行人动机。
- `partial` 输入或历史样本外回测未通过时仍保存原始分数、明细和来源，但列表与详情不再输出“动机偏弱/存在动机”等预测等级；已提议、待表决、已通过、已实施、已终止状态显示公告事实。完成回测后必须升级模型版本并显式打开校准开关。
- 下修列表只读取当前 `motive-v1.1` 快照，旧 `motive-v1` 结果保留追溯但不再混入当前视图；本地 2026-08-28 评分需重新计算后生效。
- 当前本地库基线：309只当前候选全部 `partial + unavailable`；历史评分已扩展到292个交易日，但关键输入完整率仍不足95%，说明模型未达到预测上线门槛。
- 回归证据：`server/test/convertible-bond-motive.test.js`、`server/test/convertible-bond-revision-frontend.test.js`、`server/test/convertible-bond-motive-backtest.test.js`；生产输入同步和生产发布仍需按生产流程单独授权。

## 2026-08-30 可转债下修动机评分代码链路已实现

- 按《可转债下修动机评分功能_研发方案.md》落地迁移 111：持有人持仓、持债变动、公司质押、下修周期和每日动机评分表，均支持幂等写入及评分输入快照/来源/哈希留痕。
- 新增 `convertibleBondRevisionMotiveService`，实现 `motive-v1` 五维评分、触发成熟度、可执行性、风险单列、解释项和质量状态；研究评分≥70暂显示“待历史校准”，未把评分当成确定性下修结论。
- 现有 `/api/bond-revision` 增加动机等级，新增 `/api/bond-revision/:tsCode/motive-detail` 和 `/bond-revision-motive.html?code=...`；页面只读数据库评分快照，不调用外部接口。
- 新增收盘日次日 07:20 的 `convertible_bond_revision_motive_inputs_sync`，08:15 与现有下修数学进度串行计算。未执行生产历史回填或生产同步，需用户按生产流程单独授权。
- 证据：`server/test/convertible-bond-motive.test.js`、`server/test/convertible-bond-revision-frontend.test.js`、`npm.cmd run test:all`、`npm.cmd run check:knowledge`。

## 2026-08-30 下修动机评分验收修复（本地已闭环）

- 修复评分主链实际 SQL 错误：转股价值字段补入查询，候选范围改为当前交易日行情、公开上市债券、生命周期、条款和强赎停止日的统一口径；真实本地库验收为 310/310 只评分、0 失败、结果全部落库。
- 迁移 112/113/114 补齐评分范围约束、持有人变动前后值/清仓标记、质押统计字段、触发周期首个匹配日/结束日/标签和治理检索索引；连续触发观察不再机械按 240 天拆周期。
- 修复历史时点隔离：财报、持有人、质押按公告可见日读取，正股 PB 与收盘价要求同一交易日；PostgreSQL `DATE` 按上海日历日转换，详情指定 `tradeDate` 不再偏移一天。
- 修复 Tushare 契约和单位：`top10_cb_holders` 只请求支持字段并将 `hold_amount` 万张换算为张；`pledge_stat` 只请求支持字段并将股份数量万股换算为股；同步任务增加 `ops.raw_records` 和 `ops.sync_cursors` 留痕，外部调用不包在数据库事务内。
- 详情页现在展示维度中文名、核心动机、阻断原因、指标原始值、单位、规则、变化和数据日期，不再展示来源引用或完整输入 JSON；页面仍只读评分快照，不调用外部接口，接口内部保留评分快照和来源留痕供审计。
- 修复列表动机等级全部显示“暂无数据”：`partial` 质量状态只表示部分输入缺失，仍保留已有评分并显示 `weak`/`has_motive`/`research_high`；只有 `incomplete` 才显示“暂无数据”。本地重新计算 310 只候选后，列表接口已返回实际等级，前端缓存版本更新为 44。
- 下修动机详情数值展示已统一：历史价格分位转为百分比并保留两位小数，距离到期年数保留两位小数，布尔值和状态值使用中文；详情资源缓存版本更新为 48。
- 规模比例计算项“剩余规模/发行规模”和“剩余规模/正股市值”已按百分比展示并保留两位小数，不再显示为小数倍数。
- 输入数据区的正股 20 日成交额加权均价保留两位小数，正股总市值、剩余规模和发行规模统一换算为亿元并保留两位小数。
- 输入数据区的财务指标、历史下修情况和市场每月提议下修次数已改为中文大白话说明，不再直接展示英文键名、JSON 结构或难以阅读的数组；详情资源缓存版本更新为 51。
- 验收证据：`server/test/convertible-bond-motive.test.js`、`server/test/convertible-bond-motive-frontend.test.js`、`server/test/convertible-bond-motive-db.test.js`；本地 PostgreSQL 真实评分回归通过。历史全量回填、Tushare 生产同步和生产发布仍未执行，需单独授权。

## 2026-08-30 强赎公告漏同步：补录并改造为「收盘日次日」调度（已修复并固化）

- 现象：欧通转债（123241）8-28 盘后发布「不提前赎回」公告，生产强赎监控页仍显示「已满足待确认」。
- 根因不是熔断也不是关键词：强赎公告同步原为 `weekdays: true`，只在**交易日当天** 07:45 跑一次，收盘后不补跑、周末不生成计划槽位，周五盘后的公告要拖到下周一才入库。8-28 09:09 那次跑成功（320 条）时公告尚未挂网。
- 关键词假设已证伪：默认 13 个关键词能搜到该公告（`提前赎回` 命中），`classifyCallEvent` 也能正确分类为 `waive`，PDF 解析出 `no_call_until=2026-11-28`，**同步链路代码无需修改**。
- 生产已补录并验收：备份 `/var/backups/portfolio_before_ot_ann_backfill_20260830_155238.dump`；欧通 `business_status` 由 `met_pending` → `waived`；同类核查又补上应流转债赎回结果、恒逸转2（最后交易日 2026-09-04 / 最后转股日 2026-09-09）、鸿路转债（2026-09-28 / 2026-10-08）。接口 `stale=false`、已满足待确认 1 → 0。
- **调度已根治**：新增 `afterTradingDay` 口径（`jobDefinitions.js` 声明 + `jobScheduleSlots.js` 的 `isSlotDayAllowed()`），语义是「收盘日的次日」即“前一天是交易日”。普通周落在周二至周六，节假日落在假期第一天，假期其余日期与假期后的周一都不执行。判定复用 `isWeekday`，已内置节假日数据，无需另查交易日历。适用任务：可转债安全性、下修与转股价公告事实、强赎公告、可转债行情同步、可转债估值预警；收盘后跑的任务（收盘价、净值、指数、打新日报等）维持 `weekdays` 不变。回归测试 `server/test/job-slot-after-trading-day.test.js`。
- 固化工具：`deploy/backfill_bond_call_announcements.py`（`--audit` 只读核查、`--sync --confirm-production` 备份＋补录＋验证），用法见 `docs/生产部署流程.md`。
- 已知既有问题（与本次改动无关，已用 `git stash` 验证改动前同样失败）：`server/test/tushare-routing-regression.test.js` 的熔断并发探测断言稳定失败，全量测试当前 105 通过 / 1 失败。

## 2026-08-30 可转债估值历史完整性断档（已修复）

- 原基线为行情 886 天、估值 880 天，末尾待估值 4 个交易日；核查确认 6 个缺口分别为 2 个内部断档和 4 个末尾缺口，不是行情源缺失。
- 根因是估值日常刷新只按 `ops.sync_cursors` 游标之后补偿；游标已越过内部断档时，旧逻辑不会重新发现这些日期。
- 已用现有历史回填入口补齐 2026-08-20 至 2026-08-28 的 7 个交易日窗口，当前行情/估值均为 886 天，最新日均为 2026-08-28，缺口为 0，游标已到 2026-08-28。
- `server/scripts/convertibleBondValuation.py` 已改为刷新时直接检查行情日与估值日差集；回填游标使用单调推进，补旧断档不会倒退。估值专项回归 54/54，全量测试 105/105 通过。
- 已获授权发布并同步线上：生产版本 `0.6.4.39`、提交 `79c761759d7f222a9163582e9630465668eb15c2`；线上回填 7 个交易日、1,971 只正式估值。按自 2021-01-01 的完整口径复核，行情/估值均为 1,371 天、内部缺口 0、末尾待估值 0，游标为 2026-08-28 且无错误；Web、Worker、健康检查定时器均 active，估值接口 `stale=false`。
- 详细复盘见 `docs/incidents/INC-0002-可转债估值历史完整性断档.md`。

## 2026-08-30 后台告警与外部熔断探测卡死整改（已闭环）

- 生产核查确认当前不是大量不同告警，而是 6 条仍开放记录：5 条与同一批两个历史失败计划直接相关，另有 1 条 `worker:overdue-slots` 汇总记录因这两个失败计划持续存在而重复计数。
- 两个失败计划均发生在 2026-08-28：可转债公告历史同步因 `stock-analysis` 当日额度耗尽失败；套利同步因 `cninfo` 限流熔断失败。`cninfo` 的恢复时间已过，但 `probe_in_flight` 仍为 `true`，说明 Worker 在探测期间退出后占用未释放。
- `server/services/externalCallGuard.js` 已改为“探测所有者＋一次性令牌＋5分钟租约”，异常退出后可安全回收；限流/额度耗尽的任务进入 `waiting_external`，不消耗业务重试次数；套利任务按港交所与巨潮资讯两者的最慢游标验收，可转债公告任务按公告游标自动恢复。
- Worker 心跳已固定为“主机＋角色”主记录：正常退出标记 `stopped`，异常退出按心跳判定 `stale`，健康检查定期清理超过 7 天的陈旧记录，后台概览只展示每个角色的最新记录。
- 版本 `0.6.4.38` 已完成本地代码验证并发布生产；CNInfo 游标已从已核实的 2026-08-24 自动补齐至 2026-08-30，套利补偿成功，目标活动告警已收敛为 0。Worker 当前按“主机＋角色”只保留 1 条运行记录，历史进程记录为 `stopped` 并按 7 天规则清理。

## 2026-08-30 下修监控两类漏判补强（已部署）

- 根因已确认：太能（`127108.SZ`）条款写的是“价格不低于最近一期经审计的每股净资产”，旧识别只覆盖“不得/不应低于”；斯达（`113702.SH`）触发后没有次一交易日“修正/不修正”事实，旧计算没有按上交所规则隐含重启。
- 已补充存量条款重解析迁移 `109_convertible_bond_revision_net_asset_floor_phrase`，并把上交所触发日后无次日响应统一按次一交易日重新起算；正式事件存在时不触发隐含重启。
- 本地 `2026-08-28` 全量 310/310 重算：太能为 `floor_blocked`，斯达从 `2026-08-07` 起算、已满足 6/15；同类核查当前 `floor_blocked` 共 14 只，未混入 `met_pending` 或 `near`。
- 生产已部署版本 `0.6.4.32`（提交 `430a710`）：迁移 109 已落库，`/api/bond-revision` 返回 `stale=false`、质量检查 0 待解析/0 公告错误；太能为 `floor_blocked`，斯达按重新起算后的窗口累计为 `met_pending`、20/15。
- 随后发现部署后启动补算只校验日期、不校验计算逻辑版本，已执行生产重算使斯达恢复为 `tracking`、6/15；版本 `0.6.4.40` 增加逻辑版本校验，后续规则变更会自动补算。

## 2026-08-29 发布与线上数据同步收尾

- 下修监控修复已由提交 `8fafd1cd7bf516f3b0d6a1b42cfcd8a3e8a23503` 发布，公告取源补强已由提交 `dd09ddd8ab9184de2df458ce2ba323a889d5c4f1` 发布；本次季度报告董事会锁定规则发布前线上版本为 `0.6.4.30`，本次目标版本为 `0.6.4.31`。
- 2026-08-29 线上按用户授权执行一次缓存公告重解析和下修派生重算：缓存候选 312/312 完成、7 条产生解析变化、0 条失败；2026-08-28 派生结果 310/310 完整。备份为 `/var/backups/portfolio_cb_before_reparse_20260829_214304.dump`。
- 随后按用户授权对线上多出的 7 只（`118069、123138、123142、123262、123266、127044、127098`）执行交易所官方历史公告定点补录/补解析，窗口为 `2026-04-01` 至 `2026-08-29`：7/7 成功、7/7 产生数据变化、0 条失败；备份为 `/var/backups/portfolio_cb_before_history_backfill_20260829_214936.dump`。
- 随后根据本地已落库的官方 PDF 记录，对 `123266`（2026-04-10）和 `127098`（2026-08-14）执行 v7 定点补解析并写入线上，解析结果分别锁定至 `2026-10-10`、`2026-09-13`；再次重算 310/310 完整。线上“已满足待公告”最终为 3 只（`113702、123138、127108`），其中 `123138` 的 2026-08-29 新公告覆盖了本地旧快照，故线上不会与本地 2 只完全相同。
- 本次 `0.6.4.31` 发布补强季度报告董事会前无固定日期锁定：`123138` 的公告会识别为“锁定至 2026 年第三季度报告董事会”，核查起点为 `2026-11-01`；正式发布后由每日 07:40 任务核验真实公告并回写日期。
- 事故根因、同类核查、生产补算证据与遗留风险见 `docs/incidents/INC-0001-下修计数未按公告重置.md`；本次收尾凭证见 `docs/tasks/closed/20260829-可转债下修监控发布收尾.md`。

## 2026-08-30 持仓-交易智能识别港股代码修复

- 根因：大模型可能把港股五位代码补成六位数字；统一分类层按“六位=A股”处理，历史名称特例只覆盖深圳国际，导致江西铜业股份 `00358` 可能被识别为 A 股。
- 修复：图片识别、Excel 直解析和 Excel 大模型解析统一查询 `core.instruments`；六位代码与港股五位主档/名称唯一匹配且不存在 A 股同码时，恢复五位代码、港股分类和 HKD。账本写入再做一次同样的主档校正。
- 核查：本地主档确认 `00358.HK=江西铜业股份`、`00152.HK=深圳国际`；本地持仓/交易中的 `00358` 均已是港股分类；回归测试覆盖江西铜业修正和 A 股同码保护。

## 先读什么

每个任务先阅读：

1. `AGENTS.md`：强制规则与交付标准。
2. 本文件：按任务选择权威资料。
3. `governance/knowledge-map.json`：按任务描述和实际改动文件匹配必读、必更新资料与检查。

不要默认全量读取文档。根因不明、跨模块、数据、权限、安全、任务调度或部署变更时，必须扩大阅读范围。

## 权威资料

| 主题 | 权威位置 | 何时读取/更新 |
|---|---|---|
| 技术、数据、接口、任务、配置现状 | `docs/技术架构.md` | 相关开发前阅读；现状变化时就地更新 |
| 可转债功能页视觉规范 | `docs/技术架构.md` 第 5.2 节、`public/shared/style.css` | 新增或调整强赎、下修、安全性等可转债页面时必须复用 `.bond-feature-*` 规范 |
| 生产部署、回滚、验收 | `docs/生产部署流程.md` | 部署或环境变更前阅读；流程变化时更新 |
| 用户可见版本历史 | `CHANGELOG.md`、`public/changelog.json` | 发版或用户可见变化时更新 |
| 重要长期技术取舍 | `docs/decisions/README.md` | 新增不可逆或长期影响的选择时记录 ADR |
| 重大或重复故障 | `docs/incidents/README.md` | 根因、同类范围或防复发需要沉淀时记录 Incident |
| 长任务交接 | `docs/tasks/README.md` | 跨会话、跨 Agent 或未完成工作时维护任务卡 |

## 统一工作流

开始：确认项目根、Git 基线和未提交改动；按路由读取资料；记录不确定点。

诊断：从数据源、数据库、服务、接口、页面和运行环境检查完整链路；区分直接原因、根本原因与保护机制失效原因；检查同类对象和同类功能。

收尾：用实际 `git diff --name-only` 再跑一次路由；更新命中文档；重要取舍写 ADR，重大或重复问题写 Incident；运行 `npm.cmd run check:knowledge`。

## 平台约定

Git 仓库中的本索引、`AGENTS.md` 和路由表是唯一项目事实来源。平台私有 Memory 只能辅助召回，不得写入或裁决项目事实。

| 平台 | 状态 | 接入方式 |
|---|---|---|
| Codex | 已接入（2026-07-27 起） | 工作区根 `AGENTS.md` 入口 + 项目 `.codex/config.toml` 关闭自带记忆 |
| WorkBuddy | 已接入（2026-08-29 起） | 工作区根 `AGENTS.md` 自动注入；私有记忆只做指针，规则见根目录 `AGENTS.md` 的"多 Agent 协作：记忆路径统一规则" |

机制说明：WorkBuddy 每次会话自动读取工作区根目录 `AGENTS.md`（不读 `portfolio-server/AGENTS.md`），因此路由靠根目录入口文件转发。它没有类似 Codex 的记忆开关配置项，只能靠规则约束，不要臆造配置。

未验证的平台不得宣称已接入，也不得复制本项目的事实到私有记忆。
